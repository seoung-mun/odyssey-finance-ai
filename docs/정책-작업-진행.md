# 정책 작업 진행

## 전체 진행 현황

- Stage 1A-1 — COMPLETED
- Stage 1A-2 — COMPLETED
- Preset profile 변경 — COMPLETED
- Stage 1A-3 — COMPLETED
- Stage 1B-A — COMPLETED
- Stage 1B-A2 — COMPLETED
- Stage 1B-B1 — COMPLETED
- Stage 1B-B2 — COMPLETED
- Stage 1B — COMPLETED
- Stage 2 — COMPLETED
- Stage 3A — COMPLETED
- Stage 3B — COMPLETED
- Stage 3 — IN_PROGRESS
- Stage 4 — NOT_STARTED
- Stage 5 — NOT_STARTED

## Stage 1A-1 — runtime metadata wiring

### 상태

COMPLETED

### 구현 내용

- Artifact version DTO에 application status와 region/age eligibility metadata를 추가했다.
- metadata가 있으면 enum, 판단일과 근거, 지역 코드, 연령 범위를 strict validation한다.
- metadata가 없는 기존 artifact는 계속 허용한다.
- importer가 policy version별 application status, eligibility, region code를 같은 transaction에서 동기화한다.
- reference를 exact `policyKey`, title, supportGoal로 대조한 canonical 51건 handoff를 생성했다.
- PENDING review와 non-calculable 상태를 유지했고 human approval 정보를 생성하지 않았다.

### 변경 파일

- `core-api/src/main/java/com/dacon/core/policy/PolicyDtos.java`
- `core-api/src/main/java/com/dacon/core/policy/PolicyArtifactImportService.java`
- `sql/migrations/V11__policy_runtime_metadata.sql`
- `scripts/policy_artifact/wire_runtime_metadata.js`
- `data/policy/odyssey_policy_51_runtime_metadata_handoff_v1.json`
- `core-api/src/test/java/com/dacon/core/policy/PolicyRuntimeMetadataTest.java`
- `core-api/src/test/java/com/dacon/core/policy/PolicyTestArtifacts.java`
- `core-api/src/test/java/com/dacon/core/policy/PolicyArtifactImportPostgresTest.java`

### 데이터 검증

- total 51 / mapped 51 / unmapped 0 / duplicate policyKey 0
- title/supportGoal mismatch 0
- ALLOW 23 / EXCLUDE 11 / RECHECK 17
- NATIONAL 3 / LOCAL 48
- age bounded 41 / unrestricted 10
- PENDING 51 / active calculation rule 0
- artifact 818,601 bytes
- canonical manifest PASS: `637d04ad83a6bbdad51bf588dcb37afdcd2573a0b4b896827bf1421f1933fc41`

### Migration 및 테스트

- 새 forward migration: `V11__policy_runtime_metadata.sql`
- tables: `policy_version_application_status`, `policy_version_eligibility`, `policy_version_regions`
- 기존 V4~V10은 수정하지 않았다.
- PostgreSQL 16에서 01 schema와 V4~V11 순차 적용 PASS.
- metadata focused tests PASS 3.
- 기존 `PolicySearchServiceTest` PASS 6.
- PostgreSQL importer integration PASS 7.
- Windows symlink privilege와 최초 Gradle sandbox cache 접근 실패는 product code가 아닌 ENVIRONMENT FAILURE로 기록했다.

### Git

- Commit: `5487f6e feat(core): 정책 런타임 메타데이터 연결`

## Stage 1A-2 — policy search hard filter

### 상태

COMPLETED

### 시작 상태

- branch: `checkpoint/pre-policy-20260903`
- HEAD: `5487f6e82f98831b8588340e61de4ea657029b9f`
- Stage 1A-1 commit: present
- tracked working tree: clean
- staged: none
- untracked: `.gradle/` local cache only

### 목표와 완료 항목

최종 runtime pipeline:

`supportGoal + APPROVED/effective 후보 조회 → ALLOW → region → age → cosine → Top 0~3`

- [x] search path 확인
- [x] status filter
- [x] region filter
- [x] age filter
- [x] hard-filter-before-cosine 검증
- [x] 0/1/2/최대 3건 검증
- [x] eligible 후보 간 cosine ranking 회귀 검증
- [x] PostgreSQL native query 실행 검증
- [x] 금지 영역 diff 없음 확인

### 구현 내용

- `PolicyController.search`가 인증 JWT subject의 user ID를 서비스에 전달한다.
- `PolicyRepository.activeCatalog`가 `user_profiles.birth_date`/`region_code`와 V11 version metadata를 함께 읽는다.
- 기존 SQL의 supportGoal, APPROVED, effective-date 후보 제한은 유지했다.
- `PolicySearchService`가 KST 고정 `Clock`에서 평가일을 한 번 계산하고 모든 eligibility hard filter를 cosine보다 먼저 수행한다.
- 필터를 통과한 정책만 기존 exact cosine과 기존 tie-break, 최대 3건 제한에 들어간다.
- 반환 건수를 임의로 채우지 않으므로 eligible 후보가 0/1/2개면 그대로 반환한다.

### Eligibility semantics

- Status: 정확히 `ALLOW`만 PASS. `EXCLUDE`, `RECHECK`, unknown, null/missing은 FAIL.
- Region `NATIONAL`: 사용자 region 유무와 무관하게 PASS.
- Region `LOCAL`: 사용자 `regionCode`가 non-null이고 허용 code 목록에 exact membership일 때만 PASS.
- Region missing/incomplete: FAIL이며 이름·prefix·행정구역 hierarchy를 추론하지 않는다.
- Age unrestricted: `ageMin`과 `ageMax`가 모두 null이면 birthDate 유무와 무관하게 PASS.
- Age bounded: 두 bound가 모두 있어야 하고 birthDate가 필요하다. `Period.between(birthDate, evaluationDate).getYears()`를 min/max inclusive로 비교한다.
- Age missing/incomplete, 미래 birthDate: FAIL.
- metadata 누락은 NPE/500 대신 fail closed 처리한다.

### 변경 파일

- `core-api/src/main/java/com/dacon/core/policy/PolicyController.java`: JWT user ID 전달
- `core-api/src/main/java/com/dacon/core/policy/PolicyRepository.java`: profile 및 V11 metadata 조회
- `core-api/src/main/java/com/dacon/core/policy/PolicySearchService.java`: deterministic eligibility 선필터
- `core-api/src/test/java/com/dacon/core/policy/PolicySearchServiceTest.java`: 변경된 내부 호출 계약과 기존 회귀 fixture
- `core-api/src/test/java/com/dacon/core/policy/PolicyEligibilitySearchTest.java`: status/region/age/순서/건수/ranking 집중 검증
- `core-api/src/test/java/com/dacon/core/policy/PolicyArtifactImportPostgresTest.java`: 기존 검색 회귀에 명시적 사용자 profile fixture 추가
- `core-api/src/test/java/com/dacon/core/policy/PolicySearchHttpPostgresTest.java`: 인증 사용자의 exact region/bounded-age profile fixture 추가
- `docs/정책-작업-진행.md`: Stage 1A-1 기록 통합 및 Stage 1A-2 진행·완료 기록
- `docs/policy-implementation-progress.md`: 단일 진행 문서로 통합 후 제거

### 실제 실행 테스트

- `gradlew cleanTest test --tests PolicySearchServiceTest --tests PolicyEligibilitySearchTest`: PASS, 12 tests.
- 신규 eligibility tests: PASS, 6 tests.
- 기존 search service tests: PASS, 6 tests.
- isolated PostgreSQL 16에 01 schema와 V4~V11 적용: PASS.
- `gradlew cleanTest test --tests PolicySearchHttpPostgresTest`: PASS, 2 tests.
- `gradlew spotlessApply spotlessCheck`: PASS.
- 최초 sandbox Gradle 실행은 cache JAR 접근 거부로 ENVIRONMENT FAILURE. 같은 명령의 권한 확장 재실행은 PASS.
- 임시 PostgreSQL 컨테이너는 검증 후 제거했다.

### Regression 확인

다음 영역은 변경하지 않았다.

- FRONTEND
- ANALYSIS
- Planning / Replan
- transaction / scheduled expense / onboarding financial logic
- API/OpenAPI contract
- SQL schema 및 migration
- policy data artifact
- importer production code
- calculable activation / benefit persistence

기존 supportGoal, APPROVED/effective, cosine 계산, vector 검증, query 질문 semantics와 search response 계약을 유지했다.

### Known issues / blockers

None

### 다음 확정 작업

Preset profile 변경 단계. Stage 1A-2에서는 preset을 변경하지 않는다.

## Preset profile 변경

### 상태

COMPLETED

### 시작 Git 상태

- branch: `checkpoint/pre-policy-20260903`
- HEAD: `eb32cdcd8da5d59ee816db55988054cc26deef18`
- Stage 1A-2 commit: `eb32cdc feat(core): 정책 검색 eligibility 필터 적용`
- staged/unstaged tracked files: none
- untracked: `.gradle/` local cache only

### Source of truth

- Frontend `OnboardingPage.selectDemo`: 선택한 `testerId`를 `POST /me/demo-seed`로 전달한다.
- Core `DemoService.seed`: PostgreSQL `seed_demo_user` 실행 경계이며 이번 단계의 profile 확정값을 적용할 위치다.
- DB `demo_scenarios`/`demo_transaction_templates` 및 `seed_demo_user`: 기존 transaction·financial·goal fixture를 생성한다. 기존 migration과 transaction pattern은 변경하지 않는다.
- API `DemoTester.ageGroup`은 기존 public contract의 required field이므로 제거하지 않되 preset 의미나 UI copy에는 사용하지 않는다.

### 최종 mapping

| Preset | Region | regionCode | birthDate |
|---|---|---|---|
| 변동 소비형 | 광주광역시 서구 | 12240 | 1997-01-01 |
| 균형 소비형 | 익산시 | 52140 | 1997-01-01 |
| 안정 소비형 | 제주특별자치도 제주시 | 50110 | 1991-01-01 |
| 직접 입력 | 사용자 입력 | 사용자 입력 | 사용자 입력 |

### 현재 작업

`DemoService.seed`가 기존 DB seed 결과의 testerId에 따라 demo user profile만 같은 transaction에서 갱신하도록 구현했다. tester 목록은 기존 API shape를 유지하면서 소비패턴 표시명으로 반환한다. Frontend는 소비패턴 표시명을 사용하고 MyData 안내의 연령대 표현과 raw testerId 노출을 제거했다. 직접 입력의 `/me/demo-transactions` 경로와 연령 기반 transaction template 선택 계약은 변경하지 않았다.

### 변경 파일

- `core-api/src/main/java/com/dacon/core/demo/DemoService.java`: preset별 profile 확정값과 표시명 적용
- `core-api/src/test/java/com/dacon/core/demo/DemoServiceTest.java`: 세 preset mapping과 seed profile update 검증
- `core-api/src/test/java/com/dacon/core/demo/DemoPresetProfilePostgresTest.java`: 실제 PostgreSQL seed 후 profile/transaction 보존 검증
- `core-api/src/test/java/com/dacon/core/policy/PolicyPresetReferenceSmokeTest.java`: 세 preset과 대표 정책 eligibility reference smoke 검증
- `web/src/OnboardingPage.tsx`: 소비패턴 명칭 표시 및 연령대 copy 제거
- `web/src/OnboardingPage.test.tsx`: 변경된 소비패턴 UI copy 회귀 검증
- `docs/정책-작업-진행.md`: 진행 및 완료 기록

### Runtime verification

- isolated PostgreSQL 16에 `01_schema.sql`과 V4~V11 migration을 순서대로 적용했다: PASS.
- `youth` seed 후 `user_profiles`: `birth_date=1997-01-01`, `region_code=12240`: PASS.
- `middle` seed 후 `user_profiles`: `birth_date=1997-01-01`, `region_code=52140`: PASS.
- `senior` seed 후 `user_profiles`: `birth_date=1991-01-01`, `region_code=50110`: PASS.
- 각 seed의 `demo_seed_state.tester_id`와 DEMO transaction 존재를 확인했다: PASS.
- 검증용 PostgreSQL 컨테이너는 종료 후 제거했다.

### Policy smoke verification

- 광주광역시 서구 `12240` / `1997-01-01` → `서구 청년 천원 복비(부동산 중개보수) 지원사업`: eligibility reference PASS.
- 익산시 `52140` / `1997-01-01` → `익산형 청년월세 지원사업`: eligibility reference PASS.
- 제주특별자치도 제주시 `50110` / `1991-01-01` → `제주 청년 희망충전 월세 지원`: eligibility reference PASS.
- 세 정책은 handoff에서 계속 `PENDING`이며 이번 단계에서 import/승인/활성화하지 않았다. 따라서 실제 검색 endpoint 노출 smoke는 Stage 1B 전에는 수행 대상이 아니다.

### Tests

- Core `DemoServiceTest` focused test: PASS.
- Core `DemoServiceTest` + `PolicyPresetReferenceSmokeTest`, `spotlessCheck`: PASS, 14 tests.
- Core `DemoPresetProfilePostgresTest` with actual PostgreSQL: PASS.
- Web preset 관련 filtered tests: PASS, 4 tests; 21 skipped.
- Web `npm.cmd run lint`: PASS.
- Web `npm.cmd run build`: PASS.
- 최초 PowerShell `npm test`는 execution policy로 ENVIRONMENT FAILURE였다. `npm.cmd`로 재실행했다.
- 전체 `OnboardingPage.test.tsx`는 25개 중 12개 PASS, 13개 FAIL이다. 실패는 현재 direct onboarding 흐름과 맞지 않는 기존 테스트가 수동 transaction 화면을 기대하는 baseline이며, preset 명칭/선택 관련 focused tests는 통과했다.

### Regression

- DB migration과 기존 `demo_scenarios`/`demo_transaction_templates`를 변경하지 않았다.
- 기존 testerId, transaction pattern, financial/goal/scheduled-expense seed 동작을 유지했다.
- 직접 입력 profile 및 `/me/demo-transactions`의 기존 연령 기반 template 선택 로직을 변경하지 않았다.
- Policy search/filter 구현, artifact, calculation, Planning/Replan, Analysis, API/OpenAPI를 변경하지 않았다.
- `DemoTester.ageGroup`은 public API required field여서 계약 호환 목적으로만 유지하며 UI preset 의미에는 사용하지 않는다.

### Known issues / blockers

- `OnboardingPage.test.tsx`의 direct-flow 13개 기존 테스트가 현재 화면 단계와 불일치한다. 이번 preset 범위 밖이며 focused preset 검증과 lint/build에는 영향이 없다.

### 다음 확정 작업

Stage 1B calculable ALLOW 11개 runtime activation audit부터 시작한다.

## Stage 1A-3 — informational policy exposure

### 상태

COMPLETED

- 최초 상태: `BLOCKED` — human informational approval 없음
- 해소: 2026-09-04 프로젝트 소유자가 ALLOW 23개 informational exposure를 명시적으로 승인
- 현재 상태: `COMPLETED`

### 목표

사용자가 calculable activation 이전에도 ALLOW + eligible 정책의 정보를 검색하고 읽을 수 있게 한다.

### 구현 결과

- 기존 구조는 `reviewStatus=APPROVED`인 policy version만 active snapshot과 검색 후보에 들어갈 수 있다.
- 정보 노출과 계산 활성화 자체는 `calculationMode=ELIGIBILITY_ONLY` 및 `calculationRule=null`로 분리할 수 있다. 검색 응답은 승인된 rule이 없으면 `calculationMode=INFORMATIONAL`로 제한한다.
- 원본 canonical handoff 51건은 변경하지 않았다. 프로젝트 소유자의 승인 범위에 따라 ALLOW 23개만 선택한 별도 canonical artifact를 만들고 해당 version만 `reviewStatus=APPROVED`, root를 `APPROVED/importable=true`로 기록했다.
- 승인 scope는 `INFORMATIONAL_EXPOSURE_ONLY`, reviewer representation은 `PROJECT_OWNER`, 실제 승인 기록 시각은 `2026-09-04T00:47:10Z`다. 사용자 지시 식별자를 provenance로 보존한 companion approval의 canonical SHA-256을 root `reviewGate.reason`에서 참조한다.
- EXCLUDE 11개와 RECHECK 17개는 승인 artifact에 포함하지 않아 승인·노출되지 않는다.
- 기존 frontend는 제목·요약·계획 연결·지원 내용·신청 기간·기관 공식 원문을 이미 표시한다. `ELIGIBILITY_ONLY`/`INFORMATIONAL` 결과에는 정책 비교 버튼이 나타나지 않으므로 frontend 변경도 필요하지 않다.
- 기존 search pipeline의 APPROVED/effective, ALLOW, region, age, cosine, Top 0~3 gate는 변경하지 않았다.

### 변경 파일

- `scripts/policy_artifact/approve_informational.py`: ALLOW-only 승인 artifact와 provenance companion의 결정론적 생성·검증
- `data/policy/policy-artifact-informational-approved-23.json`: import 가능한 informational policy 23건
- `data/policy/policy-artifact-informational-approved-23-approval.json`: 승인 scope/reviewer/timestamp/provenance 및 승인 대상 key 기록
- `core-api/src/test/java/com/dacon/core/policy/PolicyInformationalArtifactTest.java`: canonical importer와 승인 범위 focused 검증
- `core-api/src/test/java/com/dacon/core/policy/PolicyInformationalExposureHttpPostgresTest.java`: 실제 PostgreSQL import 및 세 preset public HTTP search 검증
- `docs/정책-작업-진행.md`: 최초 BLOCKED 이력, 승인 해소, 구현·검증 결과 기록

### Tests

- `PolicyInformationalArtifactTest` + `spotlessCheck`: PASS. 실제 importer canonical/schema validation, ALLOW 23, APPROVED, ELIGIBILITY_ONLY, null rule, provenance hash를 검증했다.
- isolated PostgreSQL 16에 현재 schema와 V4~V11 migration 적용: PASS.
- `PolicyInformationalExposureHttpPostgresTest` + `spotlessCheck`: PASS. artifact import 후 광주/익산/제주 public search endpoint가 각각 대표 정책을 Top 1~3에 포함했다.
- HTTP 결과의 `calculationMode`는 전부 `INFORMATIONAL`, 공식 URL은 nonblank, DB `policy_calculation_rules`는 0건이었다.
- 검증용 PostgreSQL 컨테이너는 제거했다.

### Known issues

None

### 다음 정확한 작업

Stage 1B — calculable ALLOW 11개 runtime activation.



## Stage 1B-A — calculable activation audit

### 상태

COMPLETED

### 목표

Calculable candidate 16개를 검증하고 ALLOW 11개의 human calculation approval 준비상태를 확정한다. 이번 단계에서는 어떤 rule도 승인하거나 활성화하지 않는다.

### Candidate count

- Total: 16
- exact `policyKey` mapping: 16/16
- ALLOW: 11
- EXCLUDE: 4
- RECHECK: 1
- ALLOW ONE_TIME_FUNDING: 9
- ALLOW MONTHLY_EXPENSE_REDUCTION: 2
- sourceVersion / locator candidate / locator SHA handoff exact match: 각각 16/16

### Importer activation contract

- Root: canonical manifest, `reviewGate.status=APPROVED`, `importable=true`, 유효한 toolchain.
- Version: `reviewStatus=APPROVED`, calculable `calculationMode`, source/chunk/provenance 완전성.
- Rule: `adjustmentType == calculationMode`, `sourceVersion == version.sourceVersion`, `approvedLocator == sourceLocator`, `approvedSha256 == locatorSha256`.
- `amountUpperBound`는 1 이상 1,000,000,000,000,000 이하이고 goldenCase는 non-empty object여야 한다.
- `humanApprovedAt`과 nonblank `reviewer`가 필요하다.
- ONE_TIME은 `maxMonths=null`, MONTHLY는 `maxMonths=1..120`이어야 한다.
- calculable mode와 calculationRule은 반드시 함께 존재해야 하며, active snapshot에는 승인된 version만 들어간다.
- Importer는 goldenCase의 업무 의미나 expected result를 실행 검증하지 않고 non-empty 여부만 검사한다. 따라서 아래 human audit에서 더 엄격하게 판정했다.

### Audit table

| # | policyKey | 정책명 | App Decision | Mode | Amount Upper Bound | Max Months | Rule Verified | Source Verified | Golden Case Verified | Activation Readiness | Blocker |
|---:|---|---|---|---|---:|---:|---|---|---|---|---|
| 1 | YOUTH_CENTER_20260325005400212269 | 2026년 전세보증금반환보증 보증료 지원사업 | ALLOW | ONE_TIME_FUNDING | 400,000 | - | YES | YES_REPO | NO | NEEDS_GOLDEN_CASE_FIX | institutionConfirmed/startYearMonth/expected adjustment 결과 없음 |
| 2 | YOUTH_CENTER_20260326005400212297 | 익산형 청년월세 지원사업 | ALLOW | MONTHLY_EXPENSE_REDUCTION | 200,000/월 | 12 | YES | YES_REPO | NO | NEEDS_GOLDEN_CASE_FIX | start/end month 및 12개월 총 2,400,000원 expected 결과 없음 |
| 3 | YOUTH_CENTER_20260406005400212449 | (강화군) 인천시 청년월세 지원사업 | EXCLUDE | MONTHLY_EXPENSE_REDUCTION | 200,000/월 | 12 | N/A | YES_REPO | N/A | NOT_ELIGIBLE | application decision EXCLUDE |
| 4 | YOUTH_CENTER_20260406005400212460 | 천원 복비(주택 중개보수 지원) | ALLOW | ONE_TIME_FUNDING | 300,000 | - | NO | YES_REPO | NO | NEEDS_RULE_FIX | 최대 30만원에서 자부담 1천원 공제인데 actual benefit cap 300,000원 허용 |
| 5 | YOUTH_CENTER_20260408005400212633 | 전세보증금반환보증 보증료 지원 | ALLOW | ONE_TIME_FUNDING | 400,000 | - | YES | YES_REPO | NO | NEEDS_GOLDEN_CASE_FIX | 가입일·유형별 실제액 입력과 expected 결과 없음 |
| 6 | YOUTH_CENTER_20260409005400212654 | 청년 월세 지원 | EXCLUDE | MONTHLY_EXPENSE_REDUCTION | 200,000/월 | 24 | N/A | YES_REPO | N/A | NOT_ELIGIBLE | application decision EXCLUDE |
| 7 | YOUTH_CENTER_20260413005400212694 | 전세보증금 반환보증 보증료 지원 | ALLOW | ONE_TIME_FUNDING | 400,000 | - | YES | YES_REPO | NO | NEEDS_GOLDEN_CASE_FIX | 실비 confirmed input과 expected 결과 없음 |
| 8 | YOUTH_CENTER_20260421005400212799 | 전세보증금반환보증 보증료 지원 | ALLOW | ONE_TIME_FUNDING | 400,000 | - | YES | YES_REPO | NO | NEEDS_GOLDEN_CASE_FIX | 가입일·유형별 실제액 입력과 expected 결과 없음 |
| 9 | YOUTH_CENTER_20260422005400212846 | 제주 청년 희망충전 월세 지원 | ALLOW | MONTHLY_EXPENSE_REDUCTION | 200,000/월 | 12 | YES | YES_REPO | NO | NEEDS_GOLDEN_CASE_FIX | start/end month 및 12개월 총 2,400,000원 expected 결과 없음 |
| 10 | YOUTH_CENTER_20260504005400113081 | 전세보증금반환보증 보증료 지원 | ALLOW | ONE_TIME_FUNDING | 400,000 | - | YES | YES_REPO | NO | NEEDS_GOLDEN_CASE_FIX | 실제 납부액 input과 expected 결과 없음 |
| 11 | YOUTH_CENTER_20260504005400213057 | 태안 청년 이사비 지원 | RECHECK | ONE_TIME_FUNDING | 400,000 | - | N/A | YES_REPO | N/A | NOT_ELIGIBLE | application decision RECHECK |
| 12 | YOUTH_CENTER_20260506005400213155 | (옹진군) 인천시 청년월세 지원사업 | EXCLUDE | MONTHLY_EXPENSE_REDUCTION | 200,000/월 | 24 | N/A | YES_REPO | N/A | NOT_ELIGIBLE | application decision EXCLUDE |
| 13 | YOUTH_CENTER_20260513005400213187 | 부산 전세보증금 반환보증 보증료 지원 | ALLOW | ONE_TIME_FUNDING | 400,000 | - | YES | YES_REPO | NO | NEEDS_GOLDEN_CASE_FIX | 가입일별 30/40만원 cap을 반영한 input/expected 결과 없음 |
| 14 | YOUTH_CENTER_20260513005400213198 | 부산 청년 중개보수 및 이사비 지원 | EXCLUDE | ONE_TIME_FUNDING | 400,000 | - | N/A | YES_REPO | N/A | NOT_ELIGIBLE | application decision EXCLUDE |
| 15 | YOUTH_CENTER_20260527005400113223 | 전세보증금반환보증 보증료 지원 | ALLOW | ONE_TIME_FUNDING | 400,000 | - | YES | YES_REPO | NO | NEEDS_GOLDEN_CASE_FIX | 가입일·유형별 실제액 input과 expected 결과 없음 |
| 16 | YOUTH_CENTER_20260806005400213321 | 서구 청년 천원 복비(부동산 중개보수) 지원사업 | ALLOW | ONE_TIME_FUNDING | 300,000 | - | NO | YES_REPO | NO | NEEDS_RULE_FIX | 최대 30만원에서 자부담 1천원 공제인데 actual benefit cap 300,000원 허용 |

`YES_REPO`는 외부 live source 재조회가 아니라 repo의 현재 handoff에서 candidate sourceVersion·locator·locator SHA가 exact match하고 `supportDetails`가 금액 의미를 뒷받침한다는 뜻이다. Candidate hash를 승인 hash로 승격하지 않았다.

### Human approval candidates

#### Calculable Human Approval Candidate — YOUTH_CENTER_20260325005400212269

- 정책명: 2026년 전세보증금반환보증 보증료 지원사업
- Application decision: ALLOW
- calculationMode / adjustmentType: ONE_TIME_FUNDING / ONE_TIME_FUNDING
- amountUpperBound / maxMonths: 400,000원 / null
- 계산 규칙: 기관이 확정한 실제 보증료 환급액을 400,000원 이하 일회성 자금으로 적용
- 공식 근거: 기납부 보증료 전부 또는 일부, 최대 40만원
- source locator / hash: `2026년 전세보증금반환보증 보증료 지원사업` / `6396f3a8c30b76552c7eaab9731b17036e2c1172ef6cdaac72d93aef1f3b8443`
- golden case draft: amountWon 400,000, expected type ONE_TIME_FUNDING
- 위험/애매한 점: institutionConfirmed, 적용 월, expected adjustment amount가 draft에 없음
- Audit verdict: NEEDS_GOLDEN_CASE_FIX

#### Calculable Human Approval Candidate — YOUTH_CENTER_20260326005400212297

- 정책명: 익산형 청년월세 지원사업
- Application decision: ALLOW
- calculationMode / adjustmentType: MONTHLY_EXPENSE_REDUCTION / MONTHLY_EXPENSE_REDUCTION
- amountUpperBound / maxMonths: 월 200,000원 / 12
- 계산 규칙: 기관 확정 월 지원액을 실제 월세 이하·월 200,000원 이하로 최대 12개월 비용 감소 적용
- 공식 근거: 월 20만원 한도, 최대 12개월, 실제 납부 월세가 낮으면 실납부액 지급
- source locator / hash: `익산형 청년월세 지원사업` / `fbdc6db51a40d9382fbb7eb8606022c90ddb5212d7f4d1440266888d98038039`
- golden case draft: amountWon 200,000, maxMonths 12, expected type MONTHLY_EXPENSE_REDUCTION
- 위험/애매한 점: start/end month 및 12개월 expected total 2,400,000원이 없음
- Audit verdict: NEEDS_GOLDEN_CASE_FIX

#### Calculable Human Approval Candidate — YOUTH_CENTER_20260406005400212460

- 정책명: 천원 복비(주택 중개보수 지원)
- Application decision: ALLOW
- calculationMode / adjustmentType: ONE_TIME_FUNDING / ONE_TIME_FUNDING
- amountUpperBound / maxMonths: 300,000원 / null
- 계산 규칙: 중개보수 실비에서 자부담 1,000원을 제외해 일회성 적용
- 공식 근거: 최대 30만원 이내 지원, 본인부담금 1천원 공제
- source locator / hash: `천원 복비(주택 중개보수 지원)` / `2f9cb7bc600a617e887e71f78eb8c367f6dc8851d29243436108473e3b468ca4`
- golden case draft: amountWon 300,000, expected type ONE_TIME_FUNDING
- 위험/애매한 점: runtime은 confirmed amount를 그대로 적용하므로 현재 cap은 300,000원 지급을 허용한다. 실제 최대 지급액 299,000원인지 규칙 수정 확인 필요
- Audit verdict: NEEDS_RULE_FIX (이후 golden case 보완도 필요)

#### Calculable Human Approval Candidate — YOUTH_CENTER_20260408005400212633

- 정책명: 전세보증금반환보증 보증료 지원
- Application decision: ALLOW
- calculationMode / adjustmentType: ONE_TIME_FUNDING / ONE_TIME_FUNDING
- amountUpperBound / maxMonths: 400,000원 / null
- 계산 규칙: 기관 확정 실제 환급액을 최대 400,000원 일회성 적용
- 공식 근거: 청년·신혼부부는 납부액, 기타 유형은 90%, 최대 40만원; 가입일에 따라 종전 30만원 가능
- source locator / hash: `전세보증금반환보증 보증료 지원` / `532946a4d8c96d466ac1e60879bac5118d155ec64b0142ae8f6c244b50445b94`
- golden case draft: amountWon 400,000, expected type ONE_TIME_FUNDING
- 위험/애매한 점: 가입일·유형별 confirmed input 및 expected 결과 사례 없음
- Audit verdict: NEEDS_GOLDEN_CASE_FIX

#### Calculable Human Approval Candidate — YOUTH_CENTER_20260413005400212694

- 정책명: 전세보증금 반환보증 보증료 지원
- Application decision: ALLOW
- calculationMode / adjustmentType: ONE_TIME_FUNDING / ONE_TIME_FUNDING
- amountUpperBound / maxMonths: 400,000원 / null
- 계산 규칙: 기관 확정 실비를 최대 400,000원 일회성 적용
- 공식 근거: 반환보증 가입비 40만원 이내 실비, 현금 지급
- source locator / hash: `전세보증금 반환보증 보증료 지원` / `5dfb0a57f168c1327bf816f1ba2ce6f1f09a9793453b02a6bb8004cb5eddd84f`
- golden case draft: amountWon 400,000, expected type ONE_TIME_FUNDING
- 위험/애매한 점: 실비 confirmed input, 적용 월, expected 결과 없음
- Audit verdict: NEEDS_GOLDEN_CASE_FIX

#### Calculable Human Approval Candidate — YOUTH_CENTER_20260421005400212799

- 정책명: 전세보증금반환보증 보증료 지원
- Application decision: ALLOW
- calculationMode / adjustmentType: ONE_TIME_FUNDING / ONE_TIME_FUNDING
- amountUpperBound / maxMonths: 400,000원 / null
- 계산 규칙: 기관 확정 실제 환급액을 최대 400,000원 일회성 적용
- 공식 근거: 기납부 보증료 전부 또는 일부, 최대 40만원; 가입일에 따라 종전 30만원 가능
- source locator / hash: `전세보증금반환보증 보증료 지원` / `532946a4d8c96d466ac1e60879bac5118d155ec64b0142ae8f6c244b50445b94`
- golden case draft: amountWon 400,000, expected type ONE_TIME_FUNDING
- 위험/애매한 점: 가입일·유형별 confirmed input 및 expected 결과 사례 없음
- Audit verdict: NEEDS_GOLDEN_CASE_FIX

#### Calculable Human Approval Candidate — YOUTH_CENTER_20260422005400212846

- 정책명: 제주 청년 희망충전 월세 지원
- Application decision: ALLOW
- calculationMode / adjustmentType: MONTHLY_EXPENSE_REDUCTION / MONTHLY_EXPENSE_REDUCTION
- amountUpperBound / maxMonths: 월 200,000원 / 12
- 계산 규칙: 기관 확정 월 지원액을 월 200,000원 이하로 최대 12개월 비용 감소 적용
- 공식 근거: 월 20만원 이하, 최대 12개월, 총 최대 240만원
- source locator / hash: `제주 청년 희망충전 월세 지원` / `28e12ece79aeceffefff9c3d56e8594eed0b7ebecf74e331a524fd8cf9c7c1da`
- golden case draft: amountWon 200,000, maxMonths 12, expected type MONTHLY_EXPENSE_REDUCTION
- 위험/애매한 점: start/end month 및 expected total 2,400,000원이 없음
- Audit verdict: NEEDS_GOLDEN_CASE_FIX

#### Calculable Human Approval Candidate — YOUTH_CENTER_20260504005400113081

- 정책명: 전세보증금반환보증 보증료 지원
- Application decision: ALLOW
- calculationMode / adjustmentType: ONE_TIME_FUNDING / ONE_TIME_FUNDING
- amountUpperBound / maxMonths: 400,000원 / null
- 계산 규칙: 기관 확정 실제 보증료 지원액을 최대 400,000원 일회성 적용
- 공식 근거: 보증료 전부 또는 일부, 최대 40만원
- source locator / hash: `전세보증금반환보증 보증료 지원` / `532946a4d8c96d466ac1e60879bac5118d155ec64b0142ae8f6c244b50445b94`
- golden case draft: amountWon 400,000, expected type ONE_TIME_FUNDING
- 위험/애매한 점: 실제 납부액 input, 적용 월, expected 결과 없음
- Audit verdict: NEEDS_GOLDEN_CASE_FIX

#### Calculable Human Approval Candidate — YOUTH_CENTER_20260513005400213187

- 정책명: 부산 전세보증금 반환보증 보증료 지원
- Application decision: ALLOW
- calculationMode / adjustmentType: ONE_TIME_FUNDING / ONE_TIME_FUNDING
- amountUpperBound / maxMonths: 400,000원 / null
- 계산 규칙: 기관 확정 실제 환급액을 최대 400,000원 일회성 적용
- 공식 근거: 기납부 보증료 전부 또는 일부, 최대 40만원; 과거 가입건 30만원 가능
- source locator / hash: `부산 전세보증금 반환보증 보증료 지원` / `711c84cf816353ac630cce59032b8aa81b87826b351f30ef2be04c5331a620e3`
- golden case draft: amountWon 400,000, expected type ONE_TIME_FUNDING
- 위험/애매한 점: 가입일별 cap을 반영한 confirmed input 및 expected 결과 사례 없음
- Audit verdict: NEEDS_GOLDEN_CASE_FIX

#### Calculable Human Approval Candidate — YOUTH_CENTER_20260527005400113223

- 정책명: 전세보증금반환보증 보증료 지원
- Application decision: ALLOW
- calculationMode / adjustmentType: ONE_TIME_FUNDING / ONE_TIME_FUNDING
- amountUpperBound / maxMonths: 400,000원 / null
- 계산 규칙: 기관 확정 실제 환급액을 최대 400,000원 일회성 적용
- 공식 근거: 기납부 보증료 최대 40만원, 청년 외는 납부액의 90%, 과거 가입건 30만원 가능
- source locator / hash: `전세보증금반환보증 보증료 지원` / `532946a4d8c96d466ac1e60879bac5118d155ec64b0142ae8f6c244b50445b94`
- golden case draft: amountWon 400,000, expected type ONE_TIME_FUNDING
- 위험/애매한 점: 가입일·유형별 confirmed input 및 expected 결과 사례 없음
- Audit verdict: NEEDS_GOLDEN_CASE_FIX

#### Calculable Human Approval Candidate — YOUTH_CENTER_20260806005400213321

- 정책명: 서구 청년 천원 복비(부동산 중개보수) 지원사업
- Application decision: ALLOW
- calculationMode / adjustmentType: ONE_TIME_FUNDING / ONE_TIME_FUNDING
- amountUpperBound / maxMonths: 300,000원 / null
- 계산 규칙: 중개보수 실비에서 자부담 1,000원을 제외해 일회성 적용
- 공식 근거: 최대 30만원 이내 지원, 본인부담금 1천원 제외
- source locator / hash: `서구 청년 천원 복비(부동산 중개보수) 지원사업` / `35311445c6b99962158424da86b14b800dcff96dec33bbbf5e25f350bb6c8591`
- golden case draft: amountWon 300,000, expected type ONE_TIME_FUNDING
- 위험/애매한 점: runtime은 confirmed amount를 그대로 적용하므로 현재 cap은 300,000원 지급을 허용한다. 실제 최대 지급액 299,000원인지 규칙 수정 확인 필요
- Audit verdict: NEEDS_RULE_FIX (이후 golden case 보완도 필요)

### Blockers

- READY_FOR_HUMAN_APPROVAL: 0개.
- NEEDS_GOLDEN_CASE_FIX: 9개. Runtime request와 동일한 institutionConfirmed, amountWon, startYearMonth 및 expected adjustment를 명시해야 한다. MONTHLY 2개는 endYearMonth와 expected month count/total도 필요하다.
- NEEDS_RULE_FIX: 2개. 천원 복비 두 정책의 `amountUpperBound=300,000`이 자부담 1,000원을 제외한 실제 최대 지원액과 일치하는지 확정·수정해야 한다.
- EXCLUDE 4개와 RECHECK 1개는 NOT_ELIGIBLE이며 계산 승인 대상이 아니다.
- repo source representation은 16건 모두 구조적으로 일치하지만 외부 live source 재검증은 수행하지 않았다.

### Usage-conscious verification

- PowerShell JSON 대조만 실행했다.
- 16/16 exact policyKey/title/supportGoal/application decision mapping을 확인했다.
- sourceVersion, locator candidate, locator SHA를 현재 runtime handoff와 대조해 각각 16/16 일치를 확인했다.
- Production code, artifact, DB, frontend를 변경하지 않았으므로 Gradle/PostgreSQL/Docker/frontend test는 실행하지 않았다.
- 현재 calculable runtime activation은 여전히 0건이다.

### 다음 정확한 작업

2개 rule cap 의미와 11개 golden case를 보완한 뒤 계산 사용에 대한 명시적 human approval을 받아 Stage 1B-B activation을 시작한다.

## Stage 1B-A2 — calculable rule / golden case preparation

### Stage status

- **COMPLETED**: ALLOW 11건의 결정론적 rule 설명, 경계 조건, 완전한 golden case 및 승인 후보 산출물을 준비했다.
- 후보 산출물: `data/policy/policy-calculable-allow-11-approval-candidates.json`
- 산출물 상태는 `READY_FOR_HUMAN_REVIEW_NOT_APPROVED`이며 `calculableApproval=NOT_GRANTED`, `runtimeActivation=false`다.
- 이 단계에서는 human approval, `reviewer`, `humanApprovedAt`, APPROVED review gate/version, runtime DB rule, import를 만들지 않았다.

### P0 explicit simplification

- `YOUTH_CENTER_20260406005400212460`, `YOUTH_CENTER_20260806005400213321`에는 `P0_EXPLICIT_SIMPLIFICATION`을 기록했다.
- P0 계산에서는 본인부담 1,000원을 별도 cashflow나 입력 금액 차감으로 반영하지 않고 `amountUpperBound=300000`과 사용자가 입력한 기관 확정 금액을 ONE_TIME으로 적용한다.
- 따라서 공식 지급액과 P0 계산 결과 사이에 최대 1,000원 차이가 날 수 있다. 이는 프로젝트 소유자의 명시적 단순화 결정이며 일반 규칙으로 확장하지 않는다.

### Rule preparation

- 대상: 16건 중 application decision `ALLOW` 11건만 포함했다. `EXCLUDE` 4건과 `RECHECK` 1건은 제외했다.
- ONE_TIME_FUNDING 9건: `institutionConfirmed=true`, `0 < amountWon <= amountUpperBound`, `startYearMonth` 필수이며 해당 월에 한 번 적용한다. `endYearMonth`는 사용하지 않는다.
- MONTHLY_EXPENSE_REDUCTION 2건: 위 조건에 더해 `endYearMonth >= startYearMonth`, 양 끝 월을 포함한 기간이 `maxMonths=12` 이하여야 하며 매월 `amountWon`을 적용한다.
- 금액 의미는 11건 모두 `INSTITUTION_CONFIRMED_ACTUAL_AMOUNT`다. 정책 상한을 자동 지급액으로 간주하지 않는다.

### Golden cases

- ONE_TIME 9건: `institutionConfirmed=true`, 상한과 같은 `amountWon`, `startYearMonth=2026-10`을 입력하고 동일 금액의 ONE_TIME adjustment가 2026-10에 한 번 생성되는 결과를 명시했다.
- MONTHLY 2건: `institutionConfirmed=true`, `amountWon=200000`, `startYearMonth=2026-10`, `endYearMonth=2027-09`를 입력하고 월 200,000원, 12개월, 명목 합계 2,400,000원의 결과를 명시했다.
- 11건 모두 input과 expected result가 존재하며 cap, 적용 월, 월수, 합계를 기계적으로 대조했다.

### Human approval candidates

공통 경계는 `amountWon <= 0` 및 cap 초과 거부, `startYearMonth` 필수다. 월별 후보는 종료월 필수·시작월 이전 거부·포함 월수 12 초과 거부이고, 일회성 후보는 종료월을 사용하지 않는다.

| policyKey | 정책명 | App | mode / adjustment | cap / months | source locator candidate | source SHA candidate | golden expected | P0 simplification | verdict |
|---|---|---|---|---|---|---|---|---|---|
| YOUTH_CENTER_20260325005400212269 | 2026년 전세보증금반환보증 보증료 지원사업 | ALLOW | ONE_TIME_FUNDING | 400,000 / null | 2026년 전세보증금반환보증 보증료 지원사업 | `6396f3a8c30b76552c7eaab9731b17036e2c1172ef6cdaac72d93aef1f3b8443` | 400,000원, 2026-10 1회 | 없음 | READY_FOR_HUMAN_APPROVAL |
| YOUTH_CENTER_20260326005400212297 | 익산형 청년월세 지원사업 | ALLOW | MONTHLY_EXPENSE_REDUCTION | 200,000 / 12 | 익산형 청년월세 지원사업 | `fbdc6db51a40d9382fbb7eb8606022c90ddb5212d7f4d1440266888d98038039` | 월 200,000원 × 12, 합계 2,400,000원 | 없음 | READY_FOR_HUMAN_APPROVAL |
| YOUTH_CENTER_20260406005400212460 | 천원 복비(주택 중개보수 지원) | ALLOW | ONE_TIME_FUNDING | 300,000 / null | 천원 복비(주택 중개보수 지원) | `2f9cb7bc600a617e887e71f78eb8c367f6dc8851d29243436108473e3b468ca4` | 300,000원, 2026-10 1회 | P0_EXPLICIT_SIMPLIFICATION | READY_FOR_HUMAN_APPROVAL |
| YOUTH_CENTER_20260408005400212633 | 전세보증금반환보증 보증료 지원 | ALLOW | ONE_TIME_FUNDING | 400,000 / null | 전세보증금반환보증 보증료 지원 | `532946a4d8c96d466ac1e60879bac5118d155ec64b0142ae8f6c244b50445b94` | 400,000원, 2026-10 1회 | 없음 | READY_FOR_HUMAN_APPROVAL |
| YOUTH_CENTER_20260413005400212694 | 전세보증금 반환보증 보증료 지원 | ALLOW | ONE_TIME_FUNDING | 400,000 / null | 전세보증금 반환보증 보증료 지원 | `5dfb0a57f168c1327bf816f1ba2ce6f1f09a9793453b02a6bb8004cb5eddd84f` | 400,000원, 2026-10 1회 | 없음 | READY_FOR_HUMAN_APPROVAL |
| YOUTH_CENTER_20260421005400212799 | 전세보증금반환보증 보증료 지원 | ALLOW | ONE_TIME_FUNDING | 400,000 / null | 전세보증금반환보증 보증료 지원 | `532946a4d8c96d466ac1e60879bac5118d155ec64b0142ae8f6c244b50445b94` | 400,000원, 2026-10 1회 | 없음 | READY_FOR_HUMAN_APPROVAL |
| YOUTH_CENTER_20260422005400212846 | 제주 청년 희망충전 월세 지원 | ALLOW | MONTHLY_EXPENSE_REDUCTION | 200,000 / 12 | 제주 청년 희망충전 월세 지원 | `28e12ece79aeceffefff9c3d56e8594eed0b7ebecf74e331a524fd8cf9c7c1da` | 월 200,000원 × 12, 합계 2,400,000원 | 없음 | READY_FOR_HUMAN_APPROVAL |
| YOUTH_CENTER_20260504005400113081 | 전세보증금반환보증 보증료 지원 | ALLOW | ONE_TIME_FUNDING | 400,000 / null | 전세보증금반환보증 보증료 지원 | `532946a4d8c96d466ac1e60879bac5118d155ec64b0142ae8f6c244b50445b94` | 400,000원, 2026-10 1회 | 없음 | READY_FOR_HUMAN_APPROVAL |
| YOUTH_CENTER_20260513005400213187 | 부산 전세보증금 반환보증 보증료 지원 | ALLOW | ONE_TIME_FUNDING | 400,000 / null | 부산 전세보증금 반환보증 보증료 지원 | `711c84cf816353ac630cce59032b8aa81b87826b351f30ef2be04c5331a620e3` | 400,000원, 2026-10 1회 | 없음 | READY_FOR_HUMAN_APPROVAL |
| YOUTH_CENTER_20260527005400113223 | 전세보증금반환보증 보증료 지원 | ALLOW | ONE_TIME_FUNDING | 400,000 / null | 전세보증금반환보증 보증료 지원 | `532946a4d8c96d466ac1e60879bac5118d155ec64b0142ae8f6c244b50445b94` | 400,000원, 2026-10 1회 | 없음 | READY_FOR_HUMAN_APPROVAL |
| YOUTH_CENTER_20260806005400213321 | 서구 청년 천원 복비(부동산 중개보수) 지원사업 | ALLOW | ONE_TIME_FUNDING | 300,000 / null | 서구 청년 천원 복비(부동산 중개보수) 지원사업 | `35311445c6b99962158424da86b14b800dcff96dec33bbbf5e25f350bb6c8591` | 300,000원, 2026-10 1회 | P0_EXPLICIT_SIMPLIFICATION | READY_FOR_HUMAN_APPROVAL |

각 행의 `sourceVersion`, 전체 input/expected JSON, 경계 조건 및 결정론적 rule 설명은 후보 JSON에 보존했다. locator/hash는 승인값이 아니라 `sourceLocatorCandidate`/`sourceShaCandidate`다.

### Final readiness

- READY_FOR_HUMAN_APPROVAL: **11/11**
- NOT_READY: **0/11**
- Golden case complete: **11/11**
- Source/provenance candidate exact match: **11/11**
- 실제 calculable 승인/활성화: **0건**

### Remaining ambiguity

- 이 준비 단계에서 남은 rule/golden/source blocker는 없다.
- 다만 11건 모두 아직 사람의 CALCULABLE 승인 전이므로 runtime에 넣을 수 없다. `READY_FOR_HUMAN_APPROVAL`은 승인 완료를 뜻하지 않는다.

### Verification

- `node scripts/policy_artifact/prepare_calculable_candidates.js`: 11건, ONE_TIME 9건, MONTHLY 2건 생성 확인.
- PowerShell JSON 구조 검사: ready 11, golden complete 11, source complete 11, P0 단순화 2, 승인 금지 필드 및 runtime activation 없음 확인.
- 사용량 지침에 따라 Gradle, PostgreSQL, Docker, frontend test는 실행하지 않았다.

### Next

11건에 대한 명시적 CALCULABLE human approval을 받은 뒤에만 Stage 1B-B activation을 시작한다.

## Stage 1B-B1 — calculable human approval / approved artifact

### 상태

**COMPLETED**

### Human approval

- Scope: `CALCULABLE_FINANCIAL_CALCULATION`
- Reviewer: `PROJECT_OWNER`
- Human approved at: `2026-09-04T01:45:29Z`
- Provenance: `explicit-project-owner-instruction:4dd7a2c2-7ef1-4465-a928-fba291af7b4d`
- Approved candidates: 11
  - ONE_TIME_FUNDING: 9
  - MONTHLY_EXPENSE_REDUCTION: 2

이번 승인은 계산 규칙 사용 승인이다. 정책 수혜 확정, 실제 계획 반영, Planning/Replan/Analysis 연계 및 runtime import 승인이 아니다.

### Approval relationship

- Informational ALLOW: 23
- Calculable approved subset: 11
- Informational-only remainder: 12
- EXCLUDE calculable: 0
- RECHECK calculable: 0
- Stage 1A-3의 `INFORMATIONAL_EXPOSURE_ONLY` 승인은 그대로 보존했으며 이번 별도 approval event로 덮어쓰지 않았다.

### P0 simplification

- 천원 복비 2건은 `P0_EXPLICIT_SIMPLIFICATION`을 승인 규칙의 일부로 보존했다.
- 1,000원 본인부담은 별도 cashflow adjustment로 모델링하지 않는다.
- `amountUpperBound=300000`, 기관 확정 `amountWon`, `0 < amountWon <= 300000`, ONE_TIME_FUNDING을 적용하며 최대 1,000원 차이를 P0에서 허용한다.

### Approved artifact

- Approval provenance: `data/policy/policy-calculable-allow-11-approval.json`
- Import-ready artifact: `data/policy/policy-calculable-allow-11-approved.json`
- Generator: `scripts/policy_artifact/approve_calculable.js`
- Candidate artifact는 덮어쓰지 않았다.
- `approvedLocator`, `approvedSha256`, approved `goldenCase`, `humanApprovedAt`, `reviewer`는 Stage 1B-A2 준비값과 이번 승인 event에서만 승격했다.
- Approval provenance canonical SHA-256: `41bdddbe37d9a796dee44a9becb112599ed9fa93b587e299c7468c2507bbad9d`
- Approved artifact manifest SHA-256: `67592ebe4b32e046a9406b8ff42c15ff64558f6636a82574a914696888fb9c3b`

### Static validation

- total/unique policyKey/source: 11/11/11
- ONE_TIME/MONTHLY: 9/2
- application decision: ALLOW only
- reviewStatus: APPROVED 11/11
- calculationRule, reviewer, humanApprovedAt, sourceVersion, approvedLocator, approvedSha256, goldenCase: complete 11/11
- ONE_TIME `maxMonths=null`, MONTHLY `maxMonths=12`: PASS
- P0 simplification: 2건
- root `reviewGate.status=APPROVED`, `importable=true`: PASS
- canonical manifest 재계산: PASS

### Runtime state

- calculation rules in DB: **0**
- Runtime activation: `NOT_YET_PERFORMED`
- PostgreSQL, Docker, Gradle, importer integration, DB import, HTTP smoke는 수행하지 않았다.
- Production code와 DB/API/frontend 계약은 변경하지 않았다.

### 다음 정확한 작업

Stage 1B-B2에서 approved calculable artifact를 실제 PostgreSQL/importer에 적용하고 runtime calculation rule 11건을 검증한다.

## Stage 1B-B2 — calculable runtime activation

### 상태

**COMPLETED**

### Approved input

- total: 11
- ONE_TIME: 9
- MONTHLY: 2
- reviewer: `PROJECT_OWNER`
- approval scope: `CALCULABLE_FINANCIAL_CALCULATION`

### Import semantics finding

- 현재 importer는 artifact 하나를 새 ACTIVE snapshot 전체로 취급한다. 따라서 11건-only B1 artifact를 직접 활성화하면 informational ALLOW catalog가 23건에서 11건으로 축소된다.
- 기존 informational snapshot이 ACTIVE인 동안 동일 policy/sourceVersion의 `calculation_mode`를 갱신하면 `protect_active_policy_version` trigger가 거부한다. 기존 snapshot retire가 importer 마지막에 있어 갱신보다 늦다.
- 정상적인 23 informational / 11 calculable 관계를 유지하려면 informational 23 전체에 승인 rule 11개만 합친 runtime activation artifact가 필요하다.
- 기존 active catalog에서 안전하게 승격하려면 동일 import transaction 안에서 기존 ACTIVE snapshot을 먼저 RETIRED로 바꾸고, version/rule을 upsert한 뒤 새 snapshot을 ACTIVE로 전환해야 한다. 실패 시 transaction rollback으로 기존 상태가 복원된다.

### Import

- B1의 11건-only 승인 artifact는 승인 근거로 보존하고 직접 snapshot으로 활성화하지 않았다.
- 기존 informational approved 23건을 바탕으로 11건의 승인 rule만 병합한 `data/policy/policy-artifact-calculable-approved-23.json`을 생성했다.
- 생성기는 `scripts/policy_artifact/prepare_calculable_activation.js`이며 기존 canonical JSON 및 manifest 규칙을 재사용한다.
- activation artifact manifest SHA-256: `65035037487bfe51e6d7a7dbd83356769371ba884ae5b1bb38c43bde6da6099c`
- `PolicyArtifactImportService`는 기존 ACTIVE snapshot을 같은 transaction 안에서 version/rule upsert 전에 RETIRED하도록 최소 변경했다. 이후 단계 실패 시 `@Transactional` rollback으로 기존 ACTIVE 상태가 복원된다.
- 기존 importer의 parse, schema validation, source/provenance validation, approval validation 및 DB upsert 경로는 그대로 사용했다.

### Runtime activation

- Isolated PostgreSQL: `postgres:16.4-alpine`
- Active artifact: `calculable-runtime-approved-23-20260904T014529Z`
- 첫 import: PASS
- 동일 artifact 두 번째 import: PASS, 동일 snapshot ID 반환
- active policies: 23
- global/active calculation rules: 11/11
- ONE_TIME_FUNDING: 9
- MONTHLY_EXPENSE_REDUCTION: 2
- informational-only: 12
- active application status: ALLOW 23, EXCLUDE 0, RECHECK 0
- golden case 및 승인 sourceVersion/locator/hash 일치: 11/11

### Representative HTTP smoke

- 광주 서구 `12240`, `MOVING_COST`: `서구 청년 천원 복비(부동산 중개보수) 지원사업`, `ONE_TIME_FUNDING` 노출 PASS
- 익산 `52140`, `MONTHLY_RENT`: `익산형 청년월세 지원사업`, `MONTHLY_EXPENSE_REDUCTION` 노출 PASS
- 제주 제주시 `50110`, `MONTHLY_RENT`: `제주 청년 희망충전 월세 지원`, `MONTHLY_EXPENSE_REDUCTION` 노출 PASS
- 세 경우 모두 실제 E2E 인증, user profile 저장, `POST /api/v1/policies/search` HTTP 경로를 사용했다.

### P0 simplification

- `YOUTH_CENTER_20260406005400212460`과 `YOUTH_CENTER_20260806005400213321`은 ONE_TIME_FUNDING, `amountUpperBound=300000`, `maxMonths=null`, reviewer `PROJECT_OWNER`로 저장됐다.
- 두 rule의 golden case에 승인된 `P0_EXPLICIT_SIMPLIFICATION`을 보존했고 focused test에서 최대 1,000원 차이 조건을 검증했다.

### Tests and regression

- `PolicyCalculableArtifactTest`: PASS — 실제 importer parse/validate, 23/11/12 및 9/2 수량, approval/source/golden/P0, reviewer 누락 거부 검증
- `PolicyCalculableActivationHttpPostgresTest`: PASS — 실제 PostgreSQL import, 재import 멱등성, 활성 수량, representative HTTP search 검증
- `PolicyArtifactImportPostgresTest`: clean isolated DB에서 전체 7 tests PASS — 기존 import, idempotency 및 rollback 회귀 검증
- `spotlessCheck`: PASS
- 정책 검색의 기존 ALLOW → region → age → cosine → Top 0~3 경로는 유지했다.
- Planning, Replan, Analysis, Web 코드는 변경하지 않았다. confirmed benefit 저장/적용도 수행하지 않았다.

### Known gap

- 현재 정책 검색 응답은 `calculationMode`를 노출하지만 rule의 `amountUpperBound`와 `maxMonths`는 노출하지 않는다. 이번 runtime activation 범위에는 포함하지 않으며 Stage 4의 API/UI 계약 작업으로 남긴다.
- B1의 11건-only artifact는 전체 snapshot 계약상 직접 활성화용이 아니다. runtime activation에는 informational-only 12건을 함께 보존한 23건 merged artifact를 사용해야 한다.

### Runtime state

- calculation rules in isolated verification DB: 11
- Runtime activation: `VERIFIED`
- 검증 종료 후 전용 PostgreSQL 컨테이너와 데이터는 제거한다.

### Next

Stage 2 — confirmed policy benefit persistence / apply API

## Stage 2 — confirmed policy benefit persistence / apply API

### 상태

**COMPLETED**

### DB

- migration: `V12__policy_benefits.sql`
- PostgreSQL 16.4 격리 DB에서 현행 schema와 V4~V12 적용 PASS
- active duplicate semantics: `(user_id, goal_id, policy_version_id) WHERE status='CONFIRMED'` partial unique index

### API

- confirm: `POST /api/v1/policy-versions/{policyVersionId}/benefits`
- read/list: `GET /api/v1/goals/{goalId}/policy-benefits` — CONFIRMED만 최신순 반환
- cancel: `DELETE /api/v1/policy-benefits/{benefitId}` — hard delete 없이 CANCELLED

### Validation

- 인증 사용자 소유 ACTIVE goal을 잠근 뒤 confirm한다. 다른 사용자 goal/list/benefit cancel은 404다.
- ACTIVE snapshot의 effective APPROVED·ALLOW calculable policy와 DB calculation rule만 허용한다.
- 검색과 confirm은 같은 `PolicyEligibilityEvaluator`로 region/age를 판정하며 cosine ranking은 confirm에 사용하지 않는다.
- `institutionConfirmed=true`, `0 < amountWon <= amountUpperBound`, ONE_TIME/MONTHLY별 월·기간 규칙을 검증한다.
- 천원 복비 2건은 cap 300,000원을 유지하고 별도 1,000원 adjustment를 만들지 않는다.

### Idempotency

- 동일 semantic request: 기존 CONFIRMED ID 반환, row count 1
- 변경된 amount/start/end: `409 POLICY_BENEFIT_CONFLICT`, 기존 행 overwrite 없음
- 같은 goal의 confirm은 비관적 goal lock으로 직렬화하고 DB partial unique index가 최종 중복을 방지한다.
- CANCELLED 이력 뒤 새 CONFIRMED row 생성 가능, active CONFIRMED는 1건 유지

### Resume QA

- ownership: 다른 사용자 goal confirm과 다른 사용자 benefit cancel 404 PASS
- idempotency: 순차·동시 exact retry 모두 같은 ID, active duplicate group 0 PASS
- conflicts: changed amount와 changed period 409 PASS
- boundaries: false confirmation, 0/cap 초과, ONE_TIME start 누락, MONTHLY end 누락·역전·13개월 거부 PASS
- cancel/reconfirm: soft cancel, 반복 cancel, list 제외, 새 ID reconfirm 및 이전 CANCELLED 보존 PASS
- PostgreSQL: 실제 승인 ONE_TIME/MONTHLY policy와 public HTTP endpoint를 사용한 3 tests PASS
- issues found/fixed: path validation이 500으로 새던 기존 공통 매핑을 `HandlerMethodValidationException` 400으로 최소 보완했다. Resume QA에서는 추가 production 결함이 없었다.

### Regression

- 기존 Policy Search의 ALLOW → region → age → cosine → Top 0~3 순서와 결과 계약을 유지했다.
- Planning, Replan, Analysis, Monte Carlo, Web 및 policy approval artifact는 변경하지 않았다.
- Stage 2 benefit을 소비하거나 `FutureCashflowAdjustment`를 만드는 코드는 아직 없다.

### Remaining gaps

- benefit 기간과 goal/plan horizon의 결합 규칙은 임의로 추가하지 않았으며 Stage 3A 통합 시 확정한다.

### Final Stage 2 status

**COMPLETED**

### Next

Stage 3A — confirmed benefit → Planning FutureCashflowAdjustment

## Stage 3A — confirmed benefit → Planning FutureCashflowAdjustment

### 상태

**COMPLETED**

### Planning input

- confirmed benefit source: 기존 Stage 2 `PolicyBenefitService.list(userId, goalId)` 경로를
  `PolicyBenefitAdjustmentReader`가 재사용한다. 이 경로는 현재 ACTIVE goal 소유권을 확인하고
  `policy_benefits.status='CONFIRMED'`인 현재 goal 행만 반환한다.
- `futureCashflowAdjustments`: `PlanInput`의 additive immutable list이며 benefit이 없거나 기존
  생성자를 사용하면 `List.of()`다. null 입력도 immutable empty list로 정규화한다.
- ordering: `startYearMonth`, `policyBenefitId` 오름차순이다.

### Mapping

- ONE_TIME: `ONE_TIME_FUNDING`의 amount/start를 그대로 옮기고 end는 null을 유지한다.
- MONTHLY: `MONTHLY_EXPENSE_REDUCTION`의 amount/start/end를 그대로 옮긴다.
- 두 유형 모두 source=`POLICY_BENEFIT`, benefit ID, policyVersionId, confirmedAt을 보존한다.
  여러 benefit은 합산하지 않는다.
- unknown type, 비양수 ID/amount, 누락된 start/confirmedAt, 유형과 맞지 않는 end는 silent
  correction 없이 fail closed한다.

### Invariants

- `currentSavedAmount`: 변경 없음
- `monthlyIncome`: 변경 없음
- `monthlyFixedCost`: 변경 없음
- Stage 3A adjustment는 Analysis 요청에 넣지 않는다. 기존 계산 입력으로 만드는
  `analysisSeed()`도 adjustment를 제외하므로 70/80/90 및 Monte Carlo 결과를 바꾸지 않는다.

### Snapshot provenance

- 새 DB migration 없이 기존 `plan_versions.policy_snapshot` JSONB의
  `futureCashflowAdjustments` 배열에 당시 adjustment 전체를 방어적 복사해 저장한다.
- benefit ID, policyVersionId, adjustmentType, amountWon, startYearMonth, endYearMonth,
  confirmedAt과 source를 보존한다.
- 저장 후 benefit이 CANCELLED가 되어도 과거 plan row의 JSON snapshot은 변경하지 않는다.
- `simulation_runs.input_snapshot`과 Analysis/FastAPI 계약은 변경하지 않았다. 따라서 strict
  policy scenario와 custom option 입력도 Stage 3A로 인해 바뀌지 않는다.

### Regression

- no benefit: immutable `[]`; 기존 Analysis 요청과 계산 의미 유지
- cancelled: Stage 2 confirmed list에서 제외되어 이후 PlanInput에 미포함
- reconfirm: Stage 2가 만든 새 CONFIRMED row만 포함하고 이전 CANCELLED row는 제외
- goal scope/soft cancel/reconfirm persistence는 Stage 2 실제 PostgreSQL HTTP 테스트의 PASS를
  재사용했다. Stage 3A는 repository SQL을 변경하지 않았다.

### Tests

- `PolicyBenefitAdjustmentReaderTest`: empty, ONE_TIME, MONTHLY, 복수 개별 provenance,
  deterministic ordering, impossible status fail-closed PASS
- `PlanningQueryServicePolicyBenefitTest`: 현재 goal adjustment 연결 및 세 금융 원본 값 불변 PASS
- `PlanningCommandServicePolicySnapshotTest`: plan policy snapshot provenance와 방어적 복사 PASS
- `PlanningServiceImplTest`: adjustment가 Analysis 요청/seed를 바꾸지 않는 회귀 포함 PASS
- focused test 14건 및 `spotlessCheck`: PASS
- repository/snapshot SQL 변경이 없어 Docker PostgreSQL은 재실행하지 않았다.

### Known gaps

- adjustment를 Analysis/Monte Carlo 현금흐름에 실제 적용하지 않는다.
- benefit confirm/cancel에 따른 Replan은 만들지 않는다.
- frontend와 public API 응답은 변경하지 않는다.

### 다음 정확한 작업

Stage 3B — FutureCashflowAdjustment → Analysis / Monte Carlo simulation

## Stage 3B — FutureCashflowAdjustment → Analysis / Monte Carlo

### 상태

**COMPLETED**

### Internal contract

- Spring: `PlanInput.futureCashflowAdjustments`를 `futureCashflowAdjustments` 배열로 보내고,
  조정이 있으면 기존 `targetDate`와 inclusive `horizonMonths`에서 역산한
  `simulationStartYearMonth`를 `YYYY-MM`으로 함께 보낸다.
- 계산 요청에는 source, policyBenefitId, policyVersionId, adjustmentType, amountWon,
  startYearMonth와 유형상 필요한 endYearMonth만 포함한다. confirmedAt은 계산에 필요하지 않아
  제외하지만 Stage 3A의 `plan_versions.policy_snapshot` provenance에는 계속 보존된다.
- FastAPI: 두 필드는 additive optional 계약이다. 기존 요청에서 필드가 없거나 명시적으로 빈
  배열이면 모두 `futureCashflowAdjustments=[]`로 처리한다. 조정이 있는데 기준 월이 없거나,
  알 수 없는 유형·출처·잘못된 기간·금액이면 fail closed한다.
- `API/openapi-internal.yaml`만 갱신했으며 public API와 DB schema는 변경하지 않았다.

### Simulation semantics

#### ONE_TIME

- `startYearMonth`와 일치하는 simulation 월에 amountWon을 positive cashflow로 정확히 한 번
  더한다. horizon 전후의 일시금은 적용하지 않는다.
- 현재 engine은 투자수익 자산 모델이 아니라 월별 누적저축 band 모델이므로 지급 월부터
  누적저축 delta가 유지되며 별도의 과거 수익률을 소급 적용하지 않는다.

#### MONTHLY

- startYearMonth부터 endYearMonth까지 양 끝을 포함해 매월 amountWon을 positive cashflow로
  더한다. `monthlyFixedCost` 자체를 변경하거나 임의 clamp하지 않는다.

#### Multiple

- 같은 월에 적용되는 여러 confirmed adjustment를 원 단위로 더하며 한 adjustment가 다른
  adjustment를 덮어쓰지 않는다. 합산은 signed int64 overflow를 거부한다.

#### Horizon

- MONTHLY는 benefit 기간과 simulation horizon의 교집합에만 적용한다. horizon 이후 시작은
  결과에 영향이 없고, 일부만 겹치면 겹치는 월만 반영한다.

### Randomness invariant

- `analysisSeed()`에 policy adjustment를 넣지 않았고 engine도 기존 randomSeed로 동일한 IID
  bootstrap 경로를 한 번 생성한다. 정책 전후 차이는 deterministic cashflow delta뿐이다.
- currentSavedAmount, monthlyIncome, monthlyFixedCost는 읽기 원본과 저장 상태 모두 변경하지
  않는다.

### 70/80/90

- 세 preset은 동일한 confirmed adjustment list를 그대로 적용한다. benefit amount를
  70/80/90 비율로 축소하거나 확대하지 않는다.
- 기존 추천 유동지출 값과 stochastic draw는 유지하고, 확인된 정책 금액은 coverage 가용액과
  월별 누적저축 band에 동일하게 반영한다.

### Empty adjustment regression

- field 누락과 명시적 `[]`의 simulate response가 동일하다. options, percentileBands,
  resultSummary, randomSeed, inputSnapshot과 inputHash가 기존 no-benefit 계산과 동일하다.
  새 default field는 adjustment가 비어 있으면 감사 snapshot에도 추가하지 않는다.

### Tests

- Spring request mapping: ONE_TIME/MONTHLY camelCase 전송, end 생략 규칙, confirmedAt 미전송,
  기준 월 역산, no-benefit `[]`, seed/금융 원본 불변을 focused test로 검증했다.
- FastAPI model/API: field 누락·빈 배열, 두 유형 parsing, 엄격한 월 형식, 기준 월 누락,
  유형별 extra field와 역전 기간 거부를 검증했다.
- Engine: exact ONE_TIME month, MONTHLY inclusive 12개월, 복수 합산, horizon 교집합,
  custom/preset 적용, fixed-seed path delta, empty regression을 검증했다.
- engine version: `1.3.0`.

### Known gaps

- simulation 시작 전 지급된 ONE_TIME을 currentSavedAmount에 자동 합산하면 double count 여부를
  판별할 provenance가 없으므로 조용히 적용하지 않는다. past ONE_TIME semantics는 후속 결정
  대상이다.
- 현재 engine에는 투자수익 자산 레이어가 없다. 지급 후 효과는 기존 누적저축 계산의
  cashflow delta로 유지된다.
- confirmed benefit의 ReplanComparison / KEEP / ACCEPT 연결은 아직 구현하지 않았다.

### 다음 정확한 작업

Stage 3C — confirmed benefit → ReplanComparison / KEEP / ACCEPT

# 남은 정책 구현 계획


현재까지 완성된 범위:

1. 51개 정책의 application status / region / age metadata runtime 연결
2. 실제 사용자 profile 기반 policy eligibility search
3. 정책 데모용 광주 / 익산 / 제주 preset profile 적용

현재 runtime 정책 검색 흐름:

`supportGoal + APPROVED/effective 후보 조회`
→ `ALLOW`
→ `region`
→ `age`
→ `cosine relevance`
→ `Top 0~3`

모든 eligibility hard filter는 cosine ranking 전에 적용된다.

현재 preset:

| Preset | Region | regionCode | birthDate |
|---|---|---|---|
| 변동 소비형 | 광주광역시 서구 | `12240` | `1997-01-01` |
| 균형 소비형 | 익산시 | `52140` | `1997-01-01` |
| 안정 소비형 | 제주특별자치도 제주시 | `50110` | `1991-01-01` |
| 직접 입력 | 사용자 입력 | 사용자 입력 | 사용자 입력 |

세 preset의 region/birthDate는 소비 유형의 연령 의미가 아니다.

세 preset은 오직 기존 transaction 소비 패턴을 의미하며, region/birthDate는 정책 데모 hard filter를 통과시키기 위한 demo profile이다.

---

# 확인 필요 사항

## OnboardingPage.test.tsx direct-flow 13 failures

전체 `OnboardingPage.test.tsx` 실행 결과:

- 전체 25 tests
- PASS 12
- FAIL 13

현재 확인된 사실:

- preset 선택/명칭 관련 focused tests PASS
- `npm.cmd run lint` PASS
- `npm.cmd run build` PASS
- 실패한 13건은 현재 direct onboarding 화면 흐름과 테스트 기대 단계가 불일치한다.
- preset 변경과 직접 관련된 focused assertion은 PASS했다.

아직 확인되지 않은 사항:

**이 13건이 preset 변경 이전 commit에서도 동일하게 실패했는지 여부**

따라서 현재 상태에서는 이를 확정적인 baseline failure라고 단정하지 않는다.

필요 시 preset 변경 직전 commit을 기준으로 같은 테스트를 실행하여 다음처럼 분류한다.

- 변경 전에도 동일한 13건 실패 → `BASELINE ISSUE`
- 변경 전 PASS / 변경 후 FAIL → `REGRESSION`

이 문제를 확인하기 전에 현재 정상 동작하는 preset 구현을 임의로 되돌리거나 unrelated frontend 코드를 수정하지 않는다.

---

# Stage 1B — calculable ALLOW 11개 runtime activation

## 상태

COMPLETED

세부 구현·승인·PostgreSQL 활성화 결과는 위 Stage 1B-A부터 Stage 1B-B2까지의 실행 기록을 따른다.

## 목표

`odyssey_policy_51_calculable_decisions_v3.json`에 존재하는 calculable 후보 중 application decision이 `ALLOW`인 정책만 실제 runtime calculable policy로 활성화한다.

현재 calculable reference 기준:

- 전체 calculable 후보 = 16
- ALLOW = 11
  - ONE_TIME_FUNDING = 9
  - MONTHLY_EXPENSE_REDUCTION = 2
- EXCLUDE = 4
- RECHECK = 1

P0에서는 **ALLOW 11개만 activation 대상**이다.

EXCLUDE 4개와 RECHECK 1개는 활성화하지 않는다.

---

## Stage 1B 시작 시 확인할 자료

우선 다음 파일을 확인한다.

`data/policy/odyssey_policy_51_calculable_decisions_v3.json`

그리고 현재 runtime importer의 실제 approval/import contract를 대조한다.

특히 다음 항목을 확인한다.

- `reviewStatus`
- `calculationMode`
- `calculationRule`
- `adjustmentType`
- `amountUpperBound`
- `maxMonths`
- `sourceVersion`
- `approvedLocator`
- `approvedSha256`
- `goldenCase`
- `humanApprovedAt`
- `reviewer`
- `reviewGate.status`
- `importable`
- provenance
- manifest

현재 reference의 다음 draft field를 runtime 승인 field와 동일하다고 가정하지 않는다.

- `approvedLocatorCandidate`
- `approvedSha256Candidate`
- `goldenCaseDraft`

---

## Human approval / provenance 원칙

사람의 승인 정보나 provenance를 임의로 생성해서 importer gate를 통과시키지 않는다.

특히 다음 값은 검증되지 않은 상태에서 임의 생성 금지:

- `reviewer`
- `humanApprovedAt`
- `approvedLocator`
- `approvedSha256`
- `goldenCase` 승인 정보
- `reviewGate` 승인 정보
- source provenance

실제 source/reference만으로 importer contract를 충족할 수 없다면 11개를 억지로 활성화하지 않는다.

이 경우:

`Stage 1B — BLOCKED: HUMAN APPROVAL / PROVENANCE REQUIRED`

로 기록하고 부족한 field와 필요한 판단을 구체적으로 적는다.

---

## Stage 1B 구현 범위

Stage 1B에서는 calculable activation까지만 수행한다.

이 단계에서 하지 않는다:

- `policy_benefits`
- 실제 benefit 저장
- 실제 Replan
- Planning input 변경
- Analysis `futureCashflowAdjustments`
- frontend 실제 지원확정 UX

---

## Stage 1B 완료 조건

- [ ] calculable 후보 16개 audit
- [ ] application decision과 calculable decision exact mapping
- [ ] ALLOW 11개만 activation 대상
- [ ] EXCLUDE 4개 비활성
- [ ] RECHECK 1개 비활성
- [ ] ONE_TIME_FUNDING 9개 contract 검증
- [ ] MONTHLY_EXPENSE_REDUCTION 2개 contract 검증
- [ ] `amountUpperBound` 검증
- [ ] `maxMonths` 검증
- [ ] `calculationRule` 검증
- [ ] golden case 검증
- [ ] approval/provenance 검증
- [ ] human approval 정보 임의 생성 없음
- [ ] 기존 Policy Search semantics 유지
- [ ] Planning/Replan 변경 없음
- [ ] Analysis 변경 없음
- [ ] focused importer/calculable tests PASS

---

# Stage 2 — confirmed policy benefit persistence / API

## 상태

NOT_STARTED

## 목표

사용자가 실제 기관으로부터 정책 지원 확정을 받은 경우 실제 지원 금액과 적용 기간을 별도 domain object로 저장한다.

정책의:

- 가상 비교
- 실제 지원 확정

은 반드시 서로 다른 상태로 취급한다.

---

## Persistence

P0 최소 구조로 신규:

`policy_benefits`

테이블을 사용한다.

필드 후보:

- `id`
- `user_id`
- `goal_id`
- `policy_version_id`
- `adjustment_type`
- `amount_won`
- `start_year_month`
- `end_year_month`
- `status`
- `confirmed_at`
- `replan_event_id` optional

status 최소:

- `CONFIRMED`
- `CANCELLED`

기존 다음 구조에 의미를 왜곡해서 저장하지 않는다.

- scheduled expense
- PlanVersion
- ReplanEvent
- financial profile

---

## 중복 방지 / idempotency

같은:

`user + goal + policyVersion`

조합에 CONFIRMED benefit이 두 개 생기지 않도록 한다.

### 동일 request 재호출

동일한 금액/기간/type request를 다시 보내면:

→ 기존 CONFIRMED 결과를 idempotent하게 반환

### conflicting duplicate

같은 policyVersion인데:

- 금액이 다름
- 기간이 다름
- adjustment type이 다름

등의 충돌이 있으면 새 benefit을 만들지 않는다.

예:

`409 BENEFIT_ALREADY_CONFIRMED`

같은 명확한 conflict semantics 사용.

---

## CANCEL

전체 수정 기능은 P1로 미룰 수 있다.

다만 잘못 입력된 실제 지원 확정을 복구할 수 있도록 최소:

`CONFIRMED → CANCELLED`

경로는 P0에서 검토한다.

CANCEL 후 과거 row를 삭제하지 않고 상태 전이로 남기는 방향을 우선한다.

---

## 권장 API

`POST /api/v1/policy-versions/{policyVersionId}/benefits`

입력 개념:

```json
{
  "goalId": 12,
  "confirmedAward": {
    "type": "MONTHLY_EXPENSE_REDUCTION",
    "institutionConfirmed": true,
    "amountWon": 150000,
    "startYearMonth": "2026-10",
    "endYearMonth": "2027-09"
  }
}
Stage 2 validation

최소 다음을 검증한다.

인증
goal ownership
active goal
policy version 존재
policy APPROVED/effective
application status = ALLOW
사용자 region eligible
사용자 age eligible
calculable rule 존재
adjustment type 일치
amountWon > 0
amountWon <= amountUpperBound
MONTHLY의 기간이 maxMonths 이하
planning horizon 범위
duplicate
idempotency
Stage 2 응답

최소 개념:

{
  "benefitId": 10,
  "status": "CONFIRMED",
  "replanEventId": 20,
  "proposedPlanVersionId": 30
}

단 Stage 2와 Stage 3를 구현 시 분리하는 경우 Stage 2에서는 persistence 결과만 반환하고 실제 Replan ID 생성은 Stage 3로 넘겨도 된다.

중요한 것은 persistence와 Replan을 무리하게 한 번에 구현하지 않는 것이다.

Stage 2 완료 조건

새 forward migration

policy_benefits table

CONFIRMED persistence

CANCEL 최소 recovery path

duplicate protection

idempotent 동일 request

conflicting duplicate 처리

goal ownership 검증

policy eligibility 검증

calculable validation

amountUpperBound 검증

maxMonths 검증

focused API tests

PostgreSQL focused integration

기존 Policy Search regression 없음

Stage 3 — confirmed benefit → Planning / Replan / Analysis
상태

NOT_STARTED

난이도

HIGH

정책 구현 중 regression 위험이 가장 큰 단계 중 하나다.

가능하면 다음 세 단계로 나눈다.

Stage 3A — Core Planning input
Stage 3B — Analysis adjustment
Stage 3C — actual Replan connection
Stage 3A — Core Planning input wiring
목표

CONFIRMED policy benefit을 일반 금융계획 계산 input에 포함한다.

실제 benefit을 financial profile 숫자 자체에 직접 합산하지 않는다.

금지:

currentSavedAmount += 지원금
monthlyIncome += 지원금
monthlyFixedCost -= 지원금

이 방식은 지원 적용 시점과 기간 정보가 사라지므로 사용하지 않는다.

반드시 미래 cashflow adjustment로 표현한다.

PlanningQueryService

현재 Planning input을 조립하는:

PlanningQueryService.input(...)

에서 해당 goal의:

CONFIRMED policy_benefits

를 조회한다.

이를:

List<FutureCashflowAdjustment>

형태로 변환한다.

PlanInput

PlanInput에:

futureCashflowAdjustments

를 추가한다.

benefit이 없는 사용자는:

futureCashflowAdjustments = []

가 된다.

policySnapshot

계획 버전의 정책 provenance에 최소 다음을 기록한다.

benefit ID
policyVersion ID
adjustment type
amount
startYearMonth
endYearMonth
calculation rule provenance

계획이 어떤 정책 지원을 전제로 계산됐는지 이후 추적할 수 있어야 한다.

Virtual / Real validation 공유

현재 virtual scenario에서 사용하는 adjustment validation과 실제 Planning이 서로 다른 계산 규칙을 가지지 않도록 한다.

필요하면 작은 shared component:

PolicyAdjustmentFactory

등으로 추출하는 것을 검토한다.

다만 Stage 3를 이유로 큰 architecture refactor를 하지 않는다.

Stage 3B — Analysis future cashflow adjustment
목표

표준:

/internal/simulate

request가 policy future adjustment를 받을 수 있게 한다.

새로운 Monte Carlo engine을 만들지 않는다.

기존 simulation engine에 future adjustment를 추가한다.

SimulateRequest

다음 field 추가:

futureCashflowAdjustments

ONE_TIME_FUNDING

특정 월에 한 번 발생하는 정책 지원금.

예:

2026-10
300,000원

해당 월의 미래 cashflow/asset path에 반영한다.

이후 자산 path에도 누적 효과가 유지되어야 한다.

MONTHLY_EXPENSE_REDUCTION

지정 기간 동안 월 지출을 감소시키는 지원.

예:

2026-10 ~ 2027-09
월 150,000원

해당 월마다 expense offset으로 반영한다.

70 / 80 / 90

70 / 80 / 90 모든 plan option에 동일한 confirmed adjustment를 적용한다.

특정 option에만 정책 지원이 적용되는 상태를 만들지 않는다.

가장 중요한 regression invariant

다음일 때:

futureCashflowAdjustments = []

기존 70/80/90 결과가 정책 기능 추가 전과 동일해야 한다.

이 조건은 자동 regression test로 둔다.

가능하면 동일 input에 대해 기존 expected output 또는 deterministic fixture와 비교한다.

Stage 3C — 실제 Replan 연결
목표

실제 policy benefit CONFIRMED 이후 기존 Replan flow를 재사용한다.

흐름:

benefit CONFIRMED
→ Replan 생성
→ ReplanComparison
→ 새 70/80/90
→ KEEP_CURRENT_PLAN / ACCEPT_NEW_PLAN
→ ACCEPT 시 option select
KEEP_CURRENT_PLAN 의미

매우 중요:

지원금 확정

과

새 계획 채택

은 서로 다른 상태다.

따라서:

benefit CONFIRMED
→ Replan
→ KEEP_CURRENT_PLAN

이어도:

benefit은 CONFIRMED 상태로 남는다.

유지되는 것은 기존 ACTIVE PlanVersion이다.

정책 지원 사실 자체가 사라지는 것이 아니다.

이후 다른 Replan

한 번 확정된 CONFIRMED benefit은 이후:

과소비 Replan
정기 Replan
계획정보 수정 Replan
기타 Replan

에서도 다시 Planning input에 포함되어야 한다.

지원 자체 취소

지원 사실을 없애려면:

policy_benefit.status = CANCELLED

같은 별도 CANCEL semantics를 사용한다.

KEEP_CURRENT_PLAN은 benefit cancel이 아니다.

benefit 없는 사용자

다음 상태인 기존 사용자:

confirmedBenefits = []
futureCashflowAdjustments = []

는 정책 기능 구현 전과 동일하게 동작해야 한다.

Stage 3 완료 조건

CONFIRMED policy benefit 조회

FutureCashflowAdjustment 생성

PlanInput 전달

Spring → Analysis request 전달

ONE_TIME semantics

MONTHLY semantics

70/80/90 모두 동일하게 적용

empty adjustment regression PASS

actual Replan 생성

ReplanComparison 연결

KEEP_CURRENT_PLAN semantics 검증

ACCEPT_NEW_PLAN semantics 검증

이후 다른 Replan에도 benefit 유지

CANCEL된 benefit 제외

benefit 없는 사용자 regression 없음

Stage 4 — Policy Frontend
상태

NOT_STARTED

목표

사용자에게 다음 네 개념을 명확히 구분해서 제공한다.

정책 탐색
정책 가상 비교
실제 정책 지원 확정
실제 정책 지원을 반영한 Replan
Policy card / detail

계산 가능한 정책은 structured field로 최소 다음 정보를 사용한다.

calculationMode
amountUpperBound
maxMonths

정책 카드 설명 문자열을 frontend에서 parsing하여 숫자를 추출하지 않는다.

PolicyResult API gap

필요한 경우 PolicyResult/API response를 확장한다.

비계산 정책
calculationMode = null
amountUpperBound = null
maxMonths = null
ONE_TIME_FUNDING
calculationMode = ONE_TIME_FUNDING
amountUpperBound = 값
maxMonths = null
MONTHLY_EXPENSE_REDUCTION
calculationMode = MONTHLY_EXPENSE_REDUCTION
amountUpperBound = 값
maxMonths = 값

필요하면 계산 가능한 정책에 badge를 표시한다.

Stage 4A — 가상 비교 UX

정책 상세에서:

정책 반영 가정으로 비교

기능을 제공한다.

현재 계획과 가상의 정책 지원 적용 계획을 비교한다.

현재 계획
vs
가상 정책 적용 계획

가상 비교에서는 실제 user state를 변경하지 않는다.

금지:

policy benefit DB 저장
PlanVersion 변경
ReplanEvent 생성
실제 ACTIVE plan 변경

현재 read-only virtual scenario semantics를 유지한다.

Stage 4B — 실제 지원 확정 UX

별도 버튼/영역:

실제 지원이 확정됐어요

를 제공한다.

ONE_TIME 입력

최소:

실제 확정 지원금
적용 월

예:

실제 확정 지원금
300,000원

적용 월
2026-10
MONTHLY 입력

최소:

실제 월 지원금
시작 월
종료 월

예:

실제 월 지원금
150,000원

시작
2026-10

종료
2027-09
Confirmation

현재 virtual scenario처럼 frontend가 자동으로:

institutionConfirmed=true

를 보내서는 안 된다.

사용자가 실제 기관에서 지원 확정을 받았다는 사실을 명시적으로 확인해야 한다.

예:

checkbox
confirmation modal
실제 계획 반영

사용자가:

실제 계획에 반영

실행 시:

actual benefit API
→ policy benefit CONFIRMED
→ Replan
→ 기존 ReplanComparison

으로 연결한다.

가능하면 기존:

KEEP_CURRENT_PLAN
ACCEPT_NEW_PLAN
select-option

UI를 재사용한다.

Stage 4 완료 조건

calculable policy structured field 표시

non-calculable policy 정상 표시

virtual compare와 actual confirmation 명확히 분리

virtual compare DB mutation 없음

실제 기관 확정 confirmation

ONE_TIME 실제 금액/월 입력

MONTHLY 실제 금액/기간 입력

benefit API 연결

ReplanComparison 연결

KEEP 정상

ACCEPT 정상

focused frontend tests PASS

lint PASS

build PASS

Stage 5 — 정책 전체 Integration / Regression Verification
상태

NOT_STARTED

목표

정책 P0 전체 기능이 통합된 상태에서 정책 기능과 기존 금융계획 기능 모두 정상 동작하는지 검증한다.

Policy import / search 검증

최소:

51 policy runtime import

ALLOW

EXCLUDE 제외

RECHECK 제외

NATIONAL region

LOCAL region

bounded age

unrestricted age

supportGoal

APPROVED/effective

cosine relevance

Top 0~3

Calculable 검증

ALLOW calculable 11개

EXCLUDE/RECHECK calculable 비활성

ONE_TIME_FUNDING 9개

MONTHLY_EXPENSE_REDUCTION 2개

amountUpperBound

maxMonths

calculationRule

approval/provenance

Virtual scenario 검증

가상 비교 정상

DB benefit 저장 없음

PlanVersion 변경 없음

ReplanEvent 생성 없음

ACTIVE plan 변경 없음

Actual benefit 검증

CONFIRMED 저장

duplicate prevention

동일 request idempotency

conflicting duplicate

CANCEL

cancelled benefit이 future planning에서 제외

Planning / Analysis 검증

ONE_TIME future adjustment

MONTHLY future adjustment

70 plan

80 plan

90 plan

empty adjustment regression

policySnapshot provenance

Replan 검증

actual benefit → Replan

ReplanComparison

KEEP_CURRENT_PLAN

ACCEPT_NEW_PLAN

select-option

KEEP 후 benefit 유지

이후 다른 Replan에서 benefit 재사용

기존 제품 Regression

정책 구현 때문에 다음 기능이 깨지지 않았는지 확인한다.

login

onboarding

demo transaction

scheduled expense CRUD

70/80/90

custom plan option

final confirmation

Dashboard

plan information edit

Replan

KEEP_CURRENT_PLAN

ACCEPT_NEW_PLAN

select-option

transaction import

과소비 demo

월 1회 정기 재계획 demo

PLAN_INFEASIBLE UX

가장 중요한 invariant

benefit 없는 일반 사용자:

confirmedBenefits = []
futureCashflowAdjustments = []

이면:

정책 기능 추가 전 기존 Planning/Replan과 동일하게 동작해야 한다.

이 조건은 최종 regression에서 반드시 확인한다.

Stage 5 권장 검증 순서

처음부터 full Docker/E2E를 무조건 돌리지 않는다.

다음 순서로 범위를 확장한다.

focused unit tests
Core policy focused integration
Analysis future-adjustment focused tests
Core ↔ Analysis focused integration
PostgreSQL migration/integration
frontend focused tests
frontend lint/build
필요한 범위의 Docker/E2E
최종 git diff / regression audit
남은 구현 로드맵 요약
Stage 1B — Calculable Activation

ALLOW인 계산가능 정책 11개만 실제 runtime calculable 상태로 안전하게 활성화한다.

핵심 위험:

human approval
provenance
golden case
importer gate
Stage 2 — Confirmed Benefit Persistence

실제 지원 확정을 policy_benefits에 저장한다.

핵심:

persistence
duplicate 방지
idempotency
CANCEL
validation
Stage 3 — Planning / Replan / Analysis

CONFIRMED benefit을 FutureCashflowAdjustment로 전달한다.

핵심:

ONE_TIME
MONTHLY
70/80/90
Replan
KEEP/ACCEPT
empty adjustment regression
Stage 4 — Frontend

가상 비교와 실제 지원 확정을 명확히 분리한다.

핵심:

calculable structured data
virtual compare
실제 지원확정 입력
confirmation
actual benefit → Replan UX
Stage 5 — Integration / Regression

정책 전체와 기존 금융계획 기능을 통합 검증한다.

작업 재개 절차

새 Codex session에서 정책 구현을 재개할 때 다음 순서로 진행한다.

docs/정책-작업-진행.md 전체 읽기
git status
현재 branch 확인
현재 HEAD 확인
현재 PR / commit history 확인
완료된 Stage를 다시 구현하지 않도록 현재 코드와 진행 문서를 최소 범위에서 대조
현재 진행할 Stage 관련 파일만 focused audit
구현
focused test
regression 확인
docs/정책-작업-진행.md 갱신
Stage 단위 commit

Stage 1B 시작 시에는 Stage 1A 전체를 다시 분석하지 않는다.

우선 다음만 확인한다.

calculable reference
현재 importer approval contract
ALLOW calculable 11개
필요한 source/provenance
작업 기록 규칙

이후 모든 정책 진행 기록은:

docs/정책-작업-진행.md

하나에 누적한다.

Codex 사용량 소진 또는 session 종료 가능성에 대비해 작업 중 다음 정보를 최신 상태로 유지한다.

현재 Stage
상태
branch
HEAD
변경 파일
완료된 구현
현재 작업 중인 파일/class/method
실제 실행 테스트
PASS / FAIL / ENVIRONMENT FAILURE
Known issues
Blocker
중요한 설계 결정
다음 정확한 작업

다음 정확한 작업은 구체적으로 적는다.

나쁜 예:

계속 구현한다.

좋은 예:

PolicyArtifactImportService의 calculable approval contract와
calculable decisions reference 16건을 policyKey 기준으로 대조하고,
ALLOW 11건에서 부족한 approval field 목록을 작성한다.
정책 구현 전체 핵심 원칙
숫자 금융 계산은 deterministic engine이 담당한다.
LLM이 금융 숫자를 임의 생성하지 않는다.
Policy Search 결과는 억지로 3개를 채우지 않는다.
EXCLUDE / RECHECK 정책은 추천 후보에 포함하지 않는다.
calculable activation에는 실제 approval/provenance가 필요하다.
human approval 정보를 Codex가 임의 생성하지 않는다.
가상 policy scenario와 실제 confirmed benefit을 구분한다.
실제 benefit을 financial profile 숫자에 직접 합산하지 않는다.
실제 benefit은 future cashflow adjustment로 표현한다.
KEEP_CURRENT_PLAN은 benefit cancel이 아니다.
KEEP_CURRENT_PLAN 이후에도 CONFIRMED benefit은 유지된다.
benefit 자체를 취소하려면 CANCEL semantics를 사용한다.
futureCashflowAdjustments = []이면 기존 계획 결과가 동일해야 한다.
기존 migration을 수정하지 않는다.
DB 변경이 필요하면 새 forward migration을 사용한다.
git add .를 사용하지 않는다.
unrelated diff/refactor를 만들지 않는다.
full Docker/E2E보다 focused verification을 우선한다.
각 Stage를 한 번에 여러 개 묶어서 구현하지 않는다.
각 Stage 종료 후 진행 문서와 Git 상태를 정리한다.
