# 성문 — AI 작업 규칙

2026 금융 AI Challenge 출품작. 소비 패턴을 분석해 목표 금액까지의 계획을 강도별로
제시하고, 실제 소비를 반영해 계속 재계산하는 동적 재무 플래너.

이 파일은 **프로젝트 전체에 적용되는 규칙과 이정표**만 담는다.
세부 규칙은 각 서비스 폴더의 `AGENTS.md`에, 작업 절차는 스킬에 있다.

## 어디를 봐야 하나

| 찾는 것 | 위치 |
|---|---|
| 무엇을 왜 만드는가 (기획·설계 결정) | `docs/기획서.md` |
| 브랜치·커밋·PR 절차 | `.claude/skills/git-flow/` |
| Python·엔진 세부 규칙 | `analysis-api/AGENTS.md` |
| 서비스 구조·명령어 | `README.md` |

## 절대 원칙 — 숫자와 LLM의 분리

이 프로젝트의 핵심 설계 결정이고, **어기면 프로젝트가 조용히 망가진다.**

- **모든 숫자**(목표 저축액, 절감률, 도달 시점, 달성확률)는 결정론적 계산 엔진과
  몬테카를로에서만 나온다.
- **LLM은 숫자를 생성하지 않는다.** 확정된 구조화 데이터(JSON)를 자연어로 풀어 쓸 뿐.
- LLM 출력의 숫자는 후처리에서 원본과 대조 검증하고, 불일치 시 재시도(상한 2~3회),
  최종 실패 시 템플릿 fallback.

계산 로직에 LLM이나 ML을 쓰자는 제안이 나오면 멈추고 되물을 것. 사칙연산과 분위수
계산에 생성형 AI를 쓰면 환각 리스크만 추가된다. 근거는 `docs/기획서.md` 5-0.

## 주석

**기본은 주석 없음.** 아래 경우에만 단다 — 분량을 채우려고 달지 말 것.

- **채택하지 않은 대안과 그 이유.** 이 프로젝트에서 제일 값나가는 주석이다.
  예: `# MCTS 아님 — 행동 선택/가지치기 없는 순수 rollout`
- **기획서 절 번호 참조.** 예: `# 기획서 5-4 폐형식 분위수` — 코드와 기획서가
  갈라지는 걸 막는 장치.
- **의도적으로 자른 코너**는 `ponytail:` 로 한계와 업그레이드 경로를 명시.
  예: `# ponytail: 전역 락, 처리량 문제되면 계좌별 락으로`
- 코드를 읽으면 아는 것(`i += 1  # i를 1 증가`)은 쓰지 않는다.
- 주석·커밋 메시지는 한국어. 식별자와 타입은 영어.
- Python 함수·메서드는 본문 첫 줄에 `"""..."""` docstring을 둔다. 입력과 반환값을
  포함해 시그니처의 역할만 한두 문장으로 설명하고 구현을 그대로 풀어 쓰지 않는다.

## 검증

로직을 추가하면 검증도 같이 남긴다. 프레임워크 없이 최소한으로:
Python 모듈은 `if __name__ == "__main__":` 안에 assert 셀프체크를 둔다
(`analysis-api/engine/*.py` 참고). 이게 이 레포의 테스트 관례다.

계산 엔진 엣지 케이스는 반드시 덮는다 — 소득 0, 지출>소득, 잔여기간 1개월,
r_max 적용 시 목표 확률 도달 불가, 100% 절감해도 목표 미달.

## 마감 제약

본선 제출 **9/7 10:00**. 심사 URL은 **9/7 11:00 ~ 9/11 23:59** 접근 가능해야 하며,
**이 기간 중 배포·부하테스트 등 안정성에 영향 주는 작업 금지** (미충족 시 결격).

## 알아둘 것

- **거래 데이터에 소득·고정비가 없다.** 공개 카드 데이터엔 카드 소비만 있어서
  월 소득과 계좌이체성 고정비(월세·대출이자)는 사용자 입력을 받아야 한다.
- **비교는 individual-level만.** 타인·또래 평균이 아니라 유저 본인의 과거와만 비교한다.
  그래서 해외 데이터를 한국 기준으로 재보정할 필요가 없다.
- **금액은 정수/NUMERIC.** float로 계산하면 반올림 오차가 쌓인다.
- `analysis-api/data/`는 gitignore됨(2.5GB+). 원본 데이터셋은 별도 공유.
- 시크릿은 `.env`(gitignore). `.env.example`을 복사해 쓴다.
- `analysis-api/bench/`는 리소스 측정 실험이지 모델 선정 근거가 아니다.
  모델은 도입 시점에 다시 테스트한다.
- 몬테카를로는 `IID_BOOTSTRAP`, 경로 수는 10,000으로 확정했다. 블록 방식은
  백테스트 결과가 나쁠 때만 재검토한다.

## 미확정 (팀 논의 대기)

DB 스키마, 공개 API 명세(OpenAPI), AWS 배포 구성, 인증 스코프, r_max 산정 기준,
재계획 임계치 %, 청년정책 API 스펙.

확정되면 `docs/기획서.md`를 먼저 갱신하고 코드를 고친다.
