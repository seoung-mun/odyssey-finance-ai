# main — 메인 에이전트 규칙

2026 금융 AI Challenge 출품작. 메인은 전체 계약과 작업 순서를 관리하고, 확정된 작업만
서비스별 에이전트에 배정한 뒤 검증·커밋한다.

## 불변조건

- 금액·절감률·도달 시점·달성확률은 결정론적 엔진과 몬테카를로에서만 나온다.
- LLM은 확정 JSON을 설명만 한다. 출력 숫자는 원본과 대조하고 실패하면 fallback한다.
- 금액은 원 단위 정수/DB `NUMERIC`이다. 비교 대상은 사용자 본인의 과거뿐이다.
- 계산에 LLM/ML을 넣거나 공용 계약을 바꾸려면 구현 전 사용자 승인을 받는다.

## 먼저 읽을 것

1. 대상 폴더의 `AGENTS.md`
2. 기획 결정: `docs/기획서.md`
3. API 계약: `API/openapi-*.yaml`
4. DB 계약: `sql/*.sql`
5. 남은 작업: `TODO.md`
6. QA 배정 시: `docs/QA-가이드.md`
7. 실제 코드, 테스트, git diff

문서 체크만으로 완료를 판단하지 않는다. 문서가 충돌하거나 설계가 없으면
`docs/미확정-설계.md`에 쟁점·근거·선택지·권장안·영향 경로를 기록하고 해당 구현만
건너뛴다. 질문은 작업 웨이브 전후에 묶어서 한다.

## 작업 순서

1. baseline과 의존관계를 확인한다.
2. 구현 전 평가 방법과 합격 조건을 정한다.
3. 확정 DB/API → CRUD → 확정 계산 → 프론트 수직 흐름 → LLM → 실측 인프라 순으로 한다.
4. 독립 작업은 동시에 최대 3개의 상위 에이전트(서비스 리드 빌더 또는 QA 리드)에
   배정한다. 서비스 리드가 호출한 하위 구현 에이전트는 이 3개에 포함하지 않는다.
5. 메인이 통합 검증하고 커밋한다.
6. 큰 덩어리가 끝나면 별도 읽기 전용 QA와 전체 회귀를 실행한다.

단순 CRUD 평가는 작업 패킷에 둔다. 금융 계산·트랜잭션·LLM·교차 서비스 변경은
`docs/evals/<task>.md`에 평가를 먼저 기록한다.

## 작업 패킷

전체 대화나 코드를 복사하지 말고 파일 경로와 작업별 차이만 전달한다.

```yaml
task: 작업명
goal: 완료 결과
read: 읽을 파일
owns: 수정 경로
contract: 입출력 계약
evaluation:
  baseline:
  acceptance:
  focused_tests:
  adversarial_cases:
  full_regression:
done: 완료 조건
verify: 검증 명령
stop: 중단 조건
report: 변경 파일, 검증 결과, 위험
```

- 에이전트는 `owns` 밖을 수정하거나 커밋하지 않는다.
- 상위 에이전트는 서비스 리드 빌더와 QA 리드를 뜻한다. 동시에 최대 3개를 운영하되,
  세션의 전체 동시 실행 한도 안에서 서비스 리드의 하위 구현 에이전트는 별도로 운영한다.
- 서비스 리드 빌더와 QA 리드는 `gpt-5.6-sol` low를 사용한다. 서비스 리드가 호출하는
  하위 구현 에이전트는 `gpt-5.6-terra` medium을 사용한다.
- 메인이 서비스 리드 빌더로 지정한 에이전트만 서로 겹치지 않는 구현 작업을 최대 2개의
  하위 에이전트에 배정할 수 있다. 리드는 각 하위 에이전트에 별도 작업 패킷과 `owns`를
  주고 결과를 검토·통합한다.
- 하위 구현 에이전트는 다른 에이전트를 호출하거나 커밋하지 않는다. QA와 서비스 리드로
  지정되지 않은 에이전트도 다른 에이전트를 호출하지 않는다.
- `API/`, `sql/`, `docs/`, `TODO.md`와 커밋은 메인만 담당한다.
- 같은 파일·계약을 여러 에이전트에 동시에 배정하지 않는다.

## QA와 수정 루프

빌더가 끝나면 상위 에이전트 슬롯에 최대 3개 읽기 전용 QA 리드를 사용한다. QA 리드는
하위 에이전트를 호출하지 않는다.
모든 QA는 `.agents/skills/adversarial-qa/` 스킬과 `docs/QA-가이드.md`를 전부 읽고 따른다.
레인은 배정 범위만 다르다.

- 계약·데이터: OpenAPI, DB, 숫자, 트랜잭션
- 적대적 서비스: 경계값, 중복·동시 요청, timeout, 인증, LLM 환각
- E2E·변경범위: 브라우저 실패 상태, 핵심 흐름, 의도 밖 파일 변경

QA는 가설 대장, `severity/evidence/reproduce/impact/owner`, 미검사 영역을 보고하고
코드를 고치지 않는다. 기준이 모호해 판정할 수 없는 항목은 결함이 아니라 사양 문제로
올리고 `docs/미확정-설계.md` 후보로 다룬다.
blocker/high는 빌더에게 재배정한다. 같은 완료 조건의 수정은 빌더·QA 합계 최대 2회다.
테스트 증거 분류, 목업 승인 경계, 실제 통합 시나리오와 보안 합격 기준은
`docs/QA-가이드.md`를 단일 기준으로 삼는다. `CONTRACT_STUB`과 `MOCK`은 메인이 작업 패킷에
명시해 별도 배정한 경우에만 사용하며 실제 통합·E2E 합격 근거로 대체하지 않는다.

P0/P1/P2, 교차 서비스 기능, 공용 계약 변경, PR·release·배포 전에는 Analysis 전체 검사,
Core `./gradlew check`, Web lint/build·브라우저 흐름과 전체 diff를 검증한다. 기존 실패는
baseline으로 기록하며 무관한 수정은 하지 않는다.

네이티브 SQL·DB 함수·제약조건을 사용하는 경로는 실제 PostgreSQL에서 검증한다. 실제 DB
검증을 실행할 수 없으면 완료 처리하지 않고 미검증 사유를 보고한다.

## Git과 승인 경계

- 브랜치: `feature/<서비스>-<내용>` (`codex`, `claude` 등 에이전트 이름 금지)
- 커밋: `.agents/skills/git-flow/SKILL.md` 준수, 메인이 검증 후 자동 수행
- push·PR·merge·release·배포는 사용자 승인 전 금지
- DB/API, 트랜잭션·멱등성·비동기 경계, 새 의존성·인프라는 사용자 승인 전 확정 금지

주석은 대안의 이유, 기획서 참조, `ponytail:` shortcut에만 쓴다. 주석·커밋은 한국어,
식별자·타입은 영어다.

심사 URL 운영 기간(2026-09-07 11:00~2026-09-11 23:59)에는 심사용 환경의 배포,
부하 테스트, 인스턴스 변경을 하지 않는다.
